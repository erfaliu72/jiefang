# 流程图（最新规则版 20260804）

> 本版沿用 5.27 原框架，为当前**最新**业务流程版本（2026-08-04），取代 20260803 版。
> 相比 20260803 版的变化：**首次付款截图前移至报单提交过程**（报单成功即完成首次付款上传，不再有报单成功后再单独发起首次付款的环节）。
>
> 最新业务规则要点：
> 1. **取消整车销售环节**，仅保留**租赁**、**以租代售**两条主流业务流程；
> 2. **指导价格判断按业务模式区分**：
>    - **以租代售（以租代购）**：老板按车型设计 n 种金融方案（首付款、每期价格、期数三维度，可自由增删改），销售报单时从老板维护的方案中选择，**无"低于指导价"特批**；方案变更后，**已出库车辆按变更前方案执行，库中新车/二手车按新方案执行**；
>    - **租赁**：**押金与月供两者都不能低于老板指导价**，任一低于 → 老板价格特批；押金 = 老板端该车型押金指导价；月供 = 标箱/宽体/高栏/冷藏/平板（5选1）价格 + 尾板（加装 +300 / 不加 0）；价格变更后，**已出库车辆按变更前价格执行，库中新车/二手车按新价格执行**；
> 3. **报单 → 出库流程顺序**：
>    - 旧顺序：销售报单 → 系统判断是否低于指导价 → 系统生成报单 → 财务确认 → 运营上传合同 → 运营发起首次付款 → 首次付款判断 → 财务首付款对账 → 车管出库
>    - 新顺序：**销售报单过程即上传客户首次付款截图**（报单提交时一个动作完成：租赁 = 押金 + 第一月月供，以租代售 = 首付款，同时以租代售需选择还款方案）→ **报单审批（一次）** → **首付款对账 + 财务报单确认** → **运营上传合同** → **车管出库** → 车辆状态：租赁中 / 以租代售
>    - 注：首次付款截图是**报单提交的一部分**，报单成功即完成首次付款上传，不存在报单成功后再单独发起首次付款的环节。
> 4. **报单审批合并为一次**：首付截图前移后，销售报单时已同时填齐押金、月租金、首付金额并上传截图，**价格特批（押金/月供低于指导价）与首付不足审批判断的是同一组报单数据**，故合并为**老板一次审批**——系统一次性展示全部异常项（价格异常 / 首付不足），老板一次决策；通过后不足部分挂应收、允许出库，驳回则报单作废、车辆回在库。
>    - **首付不足的承诺规则**：销售在报单时须填写**拖欠金额、承诺归还时间**；承诺期内（承诺归还时间之前）该拖欠部分**不计万5滞纳金**，超过承诺时间未还则转为逾期应收、按剩余欠款开始计滞纳金。
>    - **驳回后的退款闭环**：老板驳回 → 报单作废 → **老板发起退款 → 财务执行退款（打款给客户并上传退款流水号）→ 车辆释放回在库**，可重新报单。
> 5. **退车流程闭环**：
>    - 老板驳回退车 → 退车单驳回，**返回销售端修改后重新提交**，形成内部闭环；
>    - 退车退款完成后 → 车辆回**车辆管理库（在库 / 待维修）**，可**再次报单（二次租赁 或 以租代售）**，形成业务大循环。

# **一、全局规则**

```mermaid
flowchart TD
    A["车管端：上传新车 Excel 表格"] --> B["系统解析 Excel 车辆数据"]
    B --> C{"按车架号 VIN 与后台全部车辆比对"}
    C -- "VIN 已存在" --> D["判定为重复车辆<br/>不重复入库"]
    C -- "VIN 不存在" --> E["新增车辆资产<br/>车辆进入车库 / 状态：在库"]
    D --> F["导入结果汇总<br/>新增 / 重复 / 失败明细"]
    E --> F

    F --> G["销售端：新建销售报单"]
    G --> H{"销售端：选择业务模式"}
    H -- "租赁" --> I["进入租赁报单流程（见第二章）"]
    H -- "以租代售" --> J["进入以租代售报单流程（见第三章）"]

    K["老板端：维护以租代售金融方案"] --> L["按车型设计 n 种方案<br/>维度：首付款 + 每期价格 + 期数<br/>可新增 / 修改 / 删除"]
    L --> M["方案版本控制"]
    M --> M1["已出库车辆：按变更前方案执行"]
    M --> M2["库中新车 / 二手车：按新方案执行"]

    N["老板端：维护租赁指导价"] --> O["押金指导价：按车型设定"]
    N --> P["月供指导价：箱型 5 选 1<br/>（标箱 / 宽体 / 高栏 / 冷藏 / 平板）<br/>+ 尾板（加装 +300 / 不加 0）"]
    P --> Q["租赁价格版本控制"]
    Q --> M1
    Q --> M2

    R["系统规则：租赁指导价核对"] --> S["押金 ≥ 指导押金 且 月供 ≥ 指导月供"]
    S --> T["任一低于 → 价格异常，随报单一并提交老板审批"]

    U["系统规则：报单审批合并"] --> V["首次付款截图是报单提交的一部分<br/>报单成功即完成上传，无单独发起环节"]
    U --> W["租赁：押金 + 第一月月供<br/>以租代售：首付款"]
    U --> X["以租代售：报单时同时选择还款方案（无需价格特批）"]
    U --> Y["价格异常 与 首付不足<br/>合并为老板一次审批"]
    U --> Z0["首付不足时：销售填写<br/>拖欠金额、承诺归还时间<br/>承诺期内不计万5滞纳金"]
    U --> Z4["老板驳回 → 报单作废<br/>老板发起退款 → 财务打款并上传退款流水号<br/>车辆释放回在库"]

    Z["系统规则：退车回库闭环"] --> Z1["老板驳回退车 → 销售修改后重新提交"]
    Z --> Z2["退款完成 → 车辆回车辆管理库（在库/待维修）"]
    Z2 --> Z3["可再次报单：二次租赁 或 以租代售<br/>回到业务模式选择（本图 G 节点）"]
```

# 二、租赁流程

### 2.1 报单（含首付截图）→ 老板一次审批 → 财务确认 → 合同 → 出库

```mermaid
flowchart TD
    A["销售端：销售报单 / 新建报单"] --> B["选择车辆：在库车辆"]
    B --> C["销售方式：租赁"]
    C --> D["填写租赁报单数据<br/>客户、起租日、租期、押金、月租金、收款信息<br/>同时上传客户首次付款截图<br/>（首次付款 = 押金 + 第一月月供）<br/>若首付不足：填写实收金额、<br/>不足原因、拖欠部分的承诺归还时间"]
    D --> E["系统读取租赁指导价<br/>押金指导价 + 月供指导价"]
    E --> F{"系统判断（同一组报单数据）<br/>① 押金 ≥ 指导押金 且 月供 ≥ 指导月供？<br/>② 首次付款是否足额？"}

    F -- "均正常（价格达标 + 首付足额）" --> G["报单成功<br/>车辆锁定（报单锁定中）<br/>无需老板审批"]

    F -- "存在异常（任一不满足）" --> H["老板端：报单审批（一次）<br/>一次性展示全部异常项<br/>价格异常：押金/月供低于指导价<br/>首付不足：实收 < 押金+首月月供<br/>含拖欠金额 + 承诺归还时间"]

    H --> I{"老板是否通过？"}
    I -- "通过" --> J["价格异常部分特批通过<br/>首付不足部分挂账为应收<br/>承诺期内不记录万5滞纳金<br/>（承诺期 = 承诺归还时间前）<br/>允许出库"]
    I -- "驳回" --> K["报单终止 / 状态：已作废"]
    K --> K1["老板发起退款"]
    K1 --> K2["财务执行退款<br/>打款给客户并上传退款流水号"]
    K2 --> K3["车辆释放回在库"]
    K3 --> A

    G --> L["财务端：首付款对账 + 财务确认报单"]
    J --> L
    L --> M["核对实际到账<br/>填写银行流水号（≥4位）或上传回单"]
    M --> N["报单状态：已激活"]
    N --> O["运营端：上传线下合同 PDF"]
    O --> P["车管端：车辆出库<br/>上传出库照片 / 出库单"]
    P --> Q["车辆状态：租赁中"]
```

### 2.2 还款周期 → 退车 → 车辆回库（可二次报单）

```mermaid
flowchart TD
    Y["车辆状态：租赁中"] --> Z["进入每期还款与催收流程（见第四章）"]

    Z --> ZA{"还款周期结束方式"}
    ZA -- "租期到期且末期租金已收" --> AH["销售端：发起退车<br/>同时发起累计滞纳金是否收取/减免确认"]
    ZA -- "客户申请提前退车 / 收车" --> AO["销售端：发起提前退车<br/>同时发起累计滞纳金是否收取/减免确认"]

    AH --> AI["车管端：验车填单<br/>无问题确认回库，有维修需求改为待维修"]
    AO --> AI
    AI --> AJ["运营端：查询车辆相关数据并填单"]
    AJ --> AK["财务端：复核"]
    AK --> AL["老板端：领导审批"]
    AL -- "驳回" --> AM["退车单驳回<br/>返回销售端：销售修改退车信息后重新提交"]
    AM --> AH
    AL -- "通过" --> AN["财务端：退款出款<br/>填写退款流水号与金额"]
    AN --> AP["车辆状态：在库 / 待维修<br/>合同：已退车 / 已结清"]
    AP --> AQ["租赁流程结束"]
    AQ --> AR["车辆进入车辆管理库<br/>可再次报单（二次租赁 / 以租代售）<br/>重新进入第二章或第三章"]
    AR --> Y
```

# 三、以租代售流程

### 3.1 报单（选方案 + 首付截图）→ 足额判断（一次审批）→ 财务确认 → 合同 → 出库

```mermaid
flowchart TD
    A["销售端：销售报单 / 新建报单"] --> B["选择车辆：在库车辆"]
    B --> C["销售方式：以租代售"]
    C --> D["填写以租代售报单数据<br/>客户、起租日、收款信息<br/>同时上传客户首次付款截图<br/>若首付不足：填写实收金额、<br/>不足原因、拖欠部分的承诺归还时间"]
    D --> E["选择老板维护的金融方案<br/>（首付款 / 每期价格 / 期数，n 选 1）<br/>选方案本身无需价格审批"]
    E --> F{"系统判断首次付款是否足额？<br/>（实收 vs 方案首付款）"}

    F -- "足额" --> G["报单成功<br/>车辆锁定（报单锁定中）<br/>无需老板审批"]

    F -- "不足额" --> H["老板端：审批不足额出库（一次）<br/>一次性展示不足金额、原因、<br/>承诺归还时间"]

    H --> I{"老板是否通过？"}
    I -- "通过" --> J["不足部分挂账为应收<br/>承诺期（承诺归还时间前）内<br/>不记录万5滞纳金<br/>允许出库"]
    I -- "驳回" --> K["报单终止 / 状态：已作废"]
    K --> K1["老板发起退款"]
    K1 --> K2["财务执行退款<br/>打款给客户并上传退款流水号"]
    K2 --> K3["车辆释放回在库"]
    K3 --> A

    G --> L["财务端：首付款对账 + 财务确认报单"]
    J --> L
    L --> M["核对实际到账<br/>填写银行流水号（≥4位）或上传回单"]
    M --> N["报单状态：已激活"]
    N --> O["运营端：上传线下合同 PDF"]
    O --> P["车管端：车辆出库<br/>上传出库照片 / 出库单"]
    P --> Q["车辆状态：以租代售"]
```

### 3.2 初始化 → 还款周期 → 过户

```mermaid
flowchart TD
    Y["车辆状态：以租代售"] --> YA["系统按所选方案的<br/>期数和每期价格自动生成还款计划（仅首次执行一次）"]
    YA --> YB["次月开始还款"]
    YB --> Z["进入每期还款与催收流程（见第四章）"]

    Z --> ZA{"还款周期结束方式"}
    ZA -- "全部期数及金额均已还清" --> AJ["财务端：复核全部回款结清<br/>销售同步发起累计滞纳金是否收取/减免确认"]
    ZA -- "客户申请提前结清买断" --> BO["运营端：计算剩余应还本金和费用"]

    AJ --> AK["运营端：发起过户单"]
    AK --> AL["运营端：登记过户资料<br/>车辆过户至客户名下"]
    AL --> AM["车辆状态：已售 / 已过户<br/>合同：已结清"]
    AM --> AN["以租代售流程结束"]

    BO --> BP["客户一次性支付剩余全部金额<br/>填写银行流水号（≥4位）"]
    BP --> BQ["财务端：确认结清"]
    BQ --> BR["系统自动生成过户单（提前结清）"]
    BR --> AK
```

# 四、每期还款、催收与计划表调整（租赁 / 以租代售共用）

说明：本章为租赁（第二章）和以租代售（第三章）在还款阶段共用的流程。系统每日自动执行，对所有未履约还款完成的车辆订单进行状态查询、催收和滞纳金累计记录。首次付款足额的判断已前移至报单环节（见 2.1 / 3.1），本章仅处理每期还款的少还 / 多还。

### 4.1 每日监控与逾期催收

```mermaid
flowchart TD
    A["系统自动执行（每日 00:30 + 启动补跑 + 查询兜底）"] --> B["遍历所有未履约还款完成的车辆订单和挂账应收"]
    B --> C["读取还款计划表 / 挂账应收<br/>检查应还日期、应还金额、实收金额、到账状态"]
    C --> D{"当前距还款日或承诺归还日的时间节点"}

    D -- "首次付款不足挂账<br/>仍在承诺期内" --> E0["只展示待归还应收<br/>承诺期内不记录万5滞纳金"]
    D -- "首次付款不足挂账<br/>超过承诺归还日仍未归还" --> E1["转为逾期应收<br/>按剩余欠款记录万5滞纳金"]

    D -- "T-3（还款日前3天）" --> E["系统自动黄色预警"]
    E --> F["生成提醒文案<br/>尊敬的xx，您的车架号为xx的车辆将于xx日进行还款，请您知悉"]
    F --> G["标记订单状态：临近还款"]

    D -- "T 日（还款日当天）" --> H{"客户是否已还款？"}
    H -- "是" --> PAY["进入还款确认与计划表调整流程（见 4.2）"]
    H -- "否" --> N["标记订单状态：还款日 / 待催款"]

    D -- "T+3（逾期3天）" --> O["系统自动生成运营催款文案"]
    O --> P["运营上传催款单截图"]
    P --> Q["标记订单状态：T+3 运营已催款<br/>保留催款记录"]
    Q --> Q1{"客户是否已还款？"}
    Q1 -- "是" --> PAY
    Q1 -- "否" --> Q2["继续等待至 T+7"]

    D -- "T+7（逾期7天）" --> R["系统自动生成销售催款文案"]
    R --> S["销售上传催款记录 / 催款单截图"]
    S --> T["标记订单状态：T+7 销售已催款"]
    T --> T1{"客户是否已还款？"}
    T1 -- "是" --> PAY
    T1 -- "否" --> U["进入顺序核销 / 风险处理记录"]
    U --> V["1. 保险 / 服务类费用"]
    V --> W["2. 维修 / 事故 / 罚金类费用"]
    W --> X["3. 本期租金及挂账应收"]
    X --> Y["4. 逾期滞纳金仅累计记录<br/>不自动收取"]
    Y --> Z["系统记录扣款明细、顺序、金额、时间、操作人"]
    Z --> Z1["逾期满 7 天可发起锁车申请（见 4.4）"]
```

### 4.2 还款确认与计划表调整

说明：当客户在任意时间点完成付款后（无论是否逾期），均进入本流程进行确认与计划表调整。多还款沿用原规则；少还款按本版新规则处理。

```mermaid
flowchart TD
    PA["销售/运营端：点击【本期还款】或【首期对账】<br/>上传客户支付截图并填写客户实付金额"] --> PB["财务端：核销确认<br/>填写银行流水号（≥4位）或上传回单"]
    PB --> PC{"比对实际付款与标准应付金额"}

    PC -- "按标准付款" --> PD1["正常核销本期账单<br/>还款计划表不变"]

    PC -- "多于标准付款" --> PD3["沿用原多还款规则<br/>运营与客户确认抵扣月份（必填）<br/>整期覆盖 → 已还款 / 部分覆盖 → 预抵"]

    PC -- "少于标准付款" --> PG1["实付金额记录为本期还款<br/>例：应还5000，实还4000，则4000记本期已收"]
    PG1 --> PG2["差额挂账为本期应收<br/>例：1000记录为应收"]
    PG2 --> PG3["差额按万5/日统计滞纳金<br/>滞纳金仅累计记录"]
    PG3 --> PG4["本车所有流程结束后<br/>由销售发起是否收取/减免滞纳金"]

    PD1 --> PE{"是否全部还清或租期到期？"}
    PD3 --> PE
    PG4 --> PE

    PE -- "否" --> PH["继续每日监控（回到 4.1）"]
    PE -- "是" --> PI["退出还款周期，回到主业务流程<br/>租赁→退车 / 以租代售→过户"]
```

### 4.3 租金减免 / 滞纳金收取确认流程

说明：在每期分期还款计划中可触发租金减免；本车流程结束时，由销售发起累计滞纳金是否收取或减免。

```mermaid
flowchart TD
    A["销售端：发起申请"] --> B{"申请类型"}
    B -- "租金减免" --> C["填写减免原因及金额"]
    B -- "滞纳金收取/减免确认" --> D["选择收取或减免<br/>填写累计滞纳金金额、原因和说明"]

    C --> E["老板端（领导）：审批"]
    D --> E
    E --> F{"是否通过？"}
    F -- "否" --> G["驳回申请"]
    F -- "是" --> H["财务端：执行减免"]
    H --> I{"执行结果"}
    I -- "租金减免" --> J["系统按命中期数更新还款计划表<br/>减免金额在对应期数中扣除"]
    I -- "滞纳金收取" --> K["生成滞纳金应收<br/>进入收款对账"]
    I -- "滞纳金减免" --> L["冲减累计滞纳金记录<br/>同步合同应收<br/>保留审批与减免日志"]
```

### 4.4 逾期锁车流程

说明：逾期满 7 天且该期已有催收记录后，销售方可发起锁车申请。

```mermaid
flowchart TD
    A["销售端：发起锁车申请<br/>前提：逾期满7天 + 该期已有催收记录"] --> B["运营端：审核"]
    B --> C{"是否通过？"}
    C -- "驳回" --> D["锁车申请驳回<br/>车辆锁状态回退"]
    C -- "通过" --> E["老板端：审批"]
    E --> F{"是否通过？"}
    F -- "驳回" --> G["锁车申请驳回<br/>车辆锁状态回退"]
    F -- "通过" --> H["执行锁车"]
    H --> I["车辆状态：车辆已锁"]
    I --> J["客户还款后<br/>销售发起解锁申请（同样走运营审核 → 老板审批）"]
```

### 4.5 发票申请流程

说明：在租赁和以租代售的首期及每一期还款计划中均可触发，只有运营主动才可以触发。

```mermaid
flowchart TD
    A["运营端：主动触发申请发票"] --> B["填写收款公司名称、税号、开票抬头"]
    B --> C["老板端：审批"]
    C --> D{"是否通过？"}
    D -- "驳回" --> E["发票申请已驳回"]
    D -- "通过" --> F["状态：待开票"]
    F --> G["财务端：开票并填写发票号"]
    G --> H["状态：已开票"]
    H --> I{"后续操作"}
    I -- "作废" --> J["财务作废发票<br/>填写作废原因"]
    I -- "红冲" --> K["财务红冲<br/>填写红字发票号和红冲原因"]
    I -- "无" --> L["流程结束"]
```

---

## 附：版本差异对照

### 与 20260803 版差异

| 环节 | 20260803 版 | 20260804 版（当前） |
|---|---|---|
| 首次付款时机 | 报单成功后上传截图 | **首次付款截图是报单提交过程的一部分**，报单成功即完成上传，无单独发起环节 |
| 报单 → 出库顺序 | 报单 → 首付截图 → 足额判断 → 对账 + 财务确认 → 合同 → 出库 | 与 0803 版一致（首付已在报单过程内完成） |
| 报单审批次数 | 价格特批 与 首付不足审批分两次 | **合并为老板一次审批**（价格异常 + 首付不足一次性展示，一次决策） |
| 驳回后处理 | 报单作废、车辆直接回在库 | **老板发起退款 → 财务打款给客户并上传退款流水号 → 车辆回在库** |

### 与旧版（含整车销售）差异

| 环节 | 20260803 版（含整车销售） | 20260804 版（当前） |
|---|---|---|
| 业务模式 | 租赁 / 以租代售 / 整车销售 | **仅租赁 / 以租代售（整车销售已移除）** |
| 以租代售定价 | 报价 vs 整车指导价，低于走价格特批 | **老板维护 n 种金融方案（首付款 / 每期价格 / 期数），销售报单时选方案，无特批** |
| 租赁定价 | 月供 vs 租赁月供指导价（单项） | **押金 + 月供双维度均不得低于指导价**；月供 = 箱型 5 选 1 + 尾板（+300 / 0） |
| 方案 / 价格变更 | 快照版本控制 | **已出库车辆按变更前方案执行，库中新车 / 二手车按新方案执行** |
| 首次付款时机 | 运营上传合同后发起 | **报单提交过程即上传截图**（租赁 = 押金+首月月供，以租代售 = 首付款） |
| 报单审批次数 | 价格特批 与 首付不足审批分两次 | **合并为老板一次审批**（价格异常 + 首付不足一次性决策） |
| 出库前顺序 | 报单 → 价格判断 → 财务确认 → 合同 → 首付 → 对账 → 出库 | **报单（含首付截图）→ 一次审批 → 首付款对账 + 财务确认 → 合同 → 出库** |

# 8.25 诉求（第二版）全量验证报告

- 验证日期：2026-09-16
- 验证基线：8.25问题第二版（第二版未出现的诉求按已完成处理）
- 验证方式：① 自动化测试套件 70 项全量回归（pytest，全部通过）；② 六条核心流程接口级冒烟 58 项（全部通过，详见《20260916-六条核心流程冒烟测试报告》）；③ 代码逐条核对（app.py / database.py / templates/index.html）
- 测试环境：本地 Flask 开发服务器 + 独立临时 SQLite 数据库（五角色默认账号）

## 结论总览

| 分类 | 条目 | 结论 |
| --- | --- | --- |
| 本期范围（主流程/车管/销售/续租） | 第 4–31 条 | 全部已实现并验证通过 |
| 范围说明 | 第 1–3 条 | 与现状一致 |
| 明确不做/后续实施 | 第 22、32–38 条 | 符合约定（未实施或已提前实现） |

未发现阻断性问题。

---

## 一、范围（第 1–3 条）

| # | 诉求 | 结论 | 说明 |
| --- | --- | --- | --- |
| 1 | 本期调整范围：主流程、车管、销售、续租 | 符合 | 本期改动均集中在四个模块 |
| 2 | 暂缓调整：运营、财务、超管 | 符合 | 运营/财务沿用现有功能承接（见第 10 条） |
| 3 | 超管端与老板是同一人，不拆分权限 | 符合 | 系统无独立超管角色，老板角色拥有全部页面与 `['*']` 动作权限（database.py seed） |

## 二、主流程（第 4–10 条）

| # | 诉求 | 结论 | 验证证据 |
| --- | --- | --- | --- |
| 4 | 销售报单：创建意向订单、传定金截图，订单"待提交"，车辆"已锁定" | 通过 | 冒烟 F2：创建订单后 `order_status=待提交`、车辆 `status=报单锁定中`；未传定金截图创建被拦截（app.py create_sales_order） |
| 5 | 提车补款：销售二次编辑订单，传押金/首付款/租金支付截图后提交 | 通过 | PUT /api/sales-orders/<id> 支持二次编辑 `customer_screenshot_path`（附件合并）与首付实收/缺口字段后再 submit（app.py 8026 起） |
| 6 | 审批流转：老板审批→符合指导价进财务对账→运营传合同→车管传出库照片填交车日期→订单"已激活"、车辆"租赁中" | 通过 | 冒烟 F2/F3 全链路通过：符合指导价直达"待财务确认"；异常（价格/首付/黑名单）进"待老板审批"；财务确认后订单"已激活"；出库后车辆"租赁中"/"以租代售" |
| 7 | 起租日=出库日次日，系统自动生成不可人工修改 | 通过 | 冒烟 F2：出库日 2026-09-16 → `billing_start_date=2026-09-17`，由 deliver 接口服务端计算，无客户端入参（app.py 14004 deliver_vehicle） |
| 8 | 取消订单：未租赁/首付未足额可取消；销售发起、老板审批、财务填退定金流水号；定金全额退、车辆解锁；锁定不设期限 | 通过 | order-refunds 全流程：销售发起（仅本人名下、已出库拦截）→ 老板审批 → 财务填流水号（≥4 位）且退款金额必须等于应退定金 → 车辆释放回在库、订单已作废；报单锁定无到期字段（expires_at 已置 NULL，database.py 注释"历史草稿不再自动过期"） |
| 9 | 车辆成色：新车未出库仍新车；出库回库自动转二手车；二手车指导价老板维护，不影响报单 | 通过 | 退车入整备库即落"二手车"（app.py 10882）；指导价按 `is_new=新车/二手车` 分行维护且仅老板可写（冒烟 F6 验证非老板 403）；指导价不符不阻断报单、仅转老板审批 |
| 10 | 收款确认、合同上传、账单核销按现有功能承接 | 通过 | 冒烟 F4：运营传打款截图 → 财务传银行回单 → 财务核销，分摊明细可查 |

## 三、续租（第 11–17 条）

| # | 诉求 | 结论 | 验证证据 |
| --- | --- | --- | --- |
| 11 | 一个合同主档 + 多段租期，不新建合同主记录 | 通过 | renewal_applications 挂 `original_contract_id`，激活后在原合同上追加租期段与账单，不新建 contracts 记录（app.py 11339/11422） |
| 12 | 到期前发起；续租起租日固定为原到期日次日 | 通过 | `datetime.now() >= 原到期日` 发起被 400 拦截；客户端传不同起租日报错"续租起租日固定为原合同到期日次日"（_renewal_terms_from_request） |
| 13 | 老板审批后运营必须上传续租合同并填编号，否则不生成期段和账单 | 通过 | activate 缺合同文件/缺编号均 400（app.py 11447–11456） |
| 14 | 押金不足先补足；原合同有逾期允许续租旧账挂账；月租调整需老板审批 | 通过 | 押金缺口未确认到账/补缴不足均 400；发起续租不校验逾期（旧账自然挂账）；所有续租经 `create_approval_flow('renewal')` 老板审批。自动化 test_renewal_approval_deposit_topup_and_daily_billing 通过 |
| 15 | 审批跨过到期日挂"续租审批中"，车辆继续"租赁中"；通过后补生效，驳回按到期未续 | 通过 | 续租列表 display_status="续租审批中"（app.py 11327）；激活起租日仍按原到期日次日补生效；驳回不改变合同状态 |
| 16 | 全程不回库、不出库、不交车、不整备；再次续租同规则 | 通过 | 续租发起/激活路径均不触碰 vehicles.status 与整备记录；同一合同可再次发起（进行中申请去重） |
| 17 | 短租按天；正常按月；提前退车不足整月按"月租÷当月实际天数×实际使用天数" | 通过 | 续租 billing_cycle 支持 按天/按月（按天必须填天数）；`calculate_prorated_rent` 即该公式（app.py 2809），按天计费由自动化测试覆盖 |

## 四、车管（第 18–22 条）

| # | 诉求 | 结论 | 验证证据 |
| --- | --- | --- | --- |
| 18 | 车辆入库权限全部移给运营，车管不再有入库功能 | 通过 | 冒烟 F1：运营入库 200；车管/销售入库 403（POST /api/vehicles 仅 `require_role('运营','老板')`） |
| 19 | 退车照片必填：外观照、里程照、工具照 | 通过 | RETURN_FLEET_REQUIRED_FIELDS 含外观/里程/工具照片，缺失即拦截（app.py 11607 起） |
| 20 | 维修完成照片必填 | 通过 | repair/complete 缺维修后外观/里程/工具照片返回 400（app.py 10958–10968） |
| 21 | 待整备库状态：待整备/整备中/整备完成（可租可售），维修单与整备库联动，车管更新 | 通过 | 退车即建"待整备"整备记录；维修完成自动转"待整备"；整备开始/完成联动车辆状态（app.py 10849–11121） |
| 22 | "标准车型和厢型才可报单"硬校验不做 | 符合 | 报单对非标准数据仅提示/记录校验状态，不做硬阻断 |

## 五、销售（第 23–32 条）

| # | 诉求 | 结论 | 验证证据 |
| --- | --- | --- | --- |
| 23 | 报单表单：车牌号非必填；车牌号/车架号/收款公司关键词模糊搜索下拉；车架号输后几位带出；车辆信息增加车牌号选项 | 通过 | 报单表单车牌号为可创建的清空式下拉（filterable + allow-create），选中车辆自动带车牌（applyOrderVehicle）；车架号支持后缀匹配（`endsWith(cleanVin)`，index.html 6342）；收款公司为模糊搜索下拉 |
| 24 | 定金金额销售自由填写；取消全额退；锁定不设期限 | 通过 | deposit_amount 无指导价强校验（不符仅转老板审批）；取消退款额=定金全额且财务必须按应退金额支付；锁定无期限（见第 8 条） |
| 25 | 报单台账筛选：车牌号、订单状态、客户、车型、销售员 | 通过 | 冒烟 F2：五条件组合筛选命中 1 条（GET /api/sales-orders 支持 5 个筛选参数） |
| 26 | 客户应收账款：销售只看自己名下客户，展示客户/车牌/到期日/逾期天数/逾期金额 | 通过 | `_sales_contract_scope` 按报单归属过滤；自动化 test_customer_receivable_lists_respect_sales_contract_scope 通过 |
| 27 | 催收记录字段：时间/方式/对象/结果/承诺还款日/下次联系日/催收人/备注或截图 | 通过 | urge_records 表字段齐全（contacted_at、contact_method、contact_name、result、promised_repay_date、next_contact_date、operator_name、remark/evidence_path） |
| 28 | 客户库 21 个字段 | 通过 | /api/customer-library 聚合项逐项核对齐全（含历史合同数、当前租期起止、当前应收、已回款、逾期金额/天数、坏账、黑名单原因/标记人/标记时间等） |
| 29 | 客户库筛选：名称/电话/身份证/销售员/车牌/订单状态/黑名单/逾期 | 通过 | 接口支持 8 个筛选参数（is_blacklist、is_overdue 等） |
| 30 | 客户库权限：销售只看自己，老板和财务看全部 | 通过 | 销售无合同客户直接剔除 + 合同范围按归属过滤；非销售角色 `1=1` 全量 |
| 31 | 黑名单：老板标记；销售选黑名单客户报单提示原因和标记时间，仍可提交，订单自动转老板审批 | 通过 | 黑名单由 customer-blacklist POST（老板）维护；报单命中后异常文案含"客户命中黑名单：原因（标记时间 xx）"，订单可创建、提交后自动进"待老板审批"（app.py 7840–7910） |
| 32 | 佣金转车租本期不纳入 | 符合 | 系统无该功能 |

## 六、已明确但后续实施（第 33–38 条）

| # | 诉求 | 结论 | 说明 |
| --- | --- | --- | --- |
| 33 | 减免流程（销售发起→老板审核→运营填金额→财务核销→下月租金扣减→押金扣） | 已提前实现 | waivers 减免/坏账/顺延/押金抵扣已落地，自动化 test_rent_waiver_closes_the_linked_shortfall_receivable 通过；口径随财务段最终确认 |
| 34 | 应收公式：应收租金−维修减免−车型减免−特殊情况−坏账−其他 | 后续实施 | 随财务段确认后统一调整 |
| 35 | 运营段本期保持不变 | 符合 | — |
| 36 | 财务段本期保持不变（坏账抵扣口径、对账单字段、退车押金抵扣随财务段确认） | 符合 | — |
| 37 | 超管段本期不改；指导价看板、无指导价审批流程后续再调整 | 符合 | 指导价看板已对五角色可见（冒烟 F6），无指导价车辆入库已自动提醒老板维护（冒烟 F1 提示语） |
| 38 | 7.30 问题中未出现在第二版的诉求按已完成处理 | 符合 | 以此基线执行 |

## 附：验证资产

- 自动化回归：`python3 -m pytest tests/` → 70 passed（约 32s）
- 接口冒烟：58 项全部通过，结果留档 `/private/tmp/jiefang-smoke-results.json`
- 冒烟明细见《20260916-六条核心流程冒烟测试报告.md》

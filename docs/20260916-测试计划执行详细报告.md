# 测试计划执行详细报告

- 执行日期：2026-09-16
- 执行依据：《20260916-测试计划方案 v1.0》
- 被测对象：app.py（Flask 后端，148 个接口）+ templates/index.html（Vue 单页前端）+ database.py（SQLite）
- 测试基线：8.25 诉求第二版（第二版未出现的诉求按已完成处理）
- 测试环境：macOS 本机 + Python 3 + Flask test_client / 本机 HTTP 服务（127.0.0.1:49170）/ Playwright 1.60.0（Chromium 1223）
- 数据隔离：全部自动化与手工验证均在独立临时库或正式库副本（/private/tmp 下拷贝）上执行，正式库 jinjuyuan.db 零写入

---

## 一、总体结论

**本期测试结论：通过，具备发版条件（准出标准全部满足）。**

| 分层 | 计划内容 | 执行结果 | 结论 |
| --- | --- | --- | --- |
| L1/L2 单元+接口集成 | 现有 70 例 + 新增 16 例 | **86/86 通过**（38.6s） | 通过 |
| L3 接口冒烟（真实 HTTP） | 六条核心流程 58 项 | **58/58 通过** | 通过 |
| L4 前端 E2E（Playwright 新引入） | 五角色登录+菜单矩阵、移动端检查 | **8/8 通过** | 通过（首批落地） |
| 权限矩阵（P0） | 路由×角色穷举 + 实测 | 静态 145 条规则提取；P0 19 接口 × 5 角色实测 **95/95 通过** | 通过（发现 3 个未鉴权只读接口，已登记） |
| 数据与迁移 | init_db 幂等、MySQL 适配 | 正式库副本两轮初始化行数变化 0；MySQL 适配层 6 例通过 | 通过 |
| 非功能摸底 | 性能基准、安全基线 | 完成摸底；发现 1 项性能超标（工作台约 5.8s）与 1 项安全登记项 | 摸底完成，不阻断本期 |

缺陷：本轮新发现 3 项（1 项已修复并回归通过，2 项登记跟踪）；历史 8.25 诉求验证 38/38 通过结论维持不变。

---

## 二、L1/L2 自动化回归明细（pytest，86/86）

执行命令：`python3 -m pytest tests/ -q`，结果 `86 passed in 38.61s`。

| 测试文件 | 用例数 | 覆盖域 |
| --- | --- | --- |
| tests/test_august_full_lease_rent_to_buy_flow.py | 17 | 租赁/以租代售全流程（报单、审批、财务、合同、出库、账单、退车） |
| tests/test_plan_20260916_api.py | 16 | 本计划新增 API-01～16（见第三章明细） |
| tests/test_sku_document_alignment.py | 13 | 多车（SKU）报单与文档口径对齐 |
| tests/test_august_lease_rent_to_buy_acceptance.py | 11 | 租赁/以租代售验收场景（续租、折算、提前结清） |
| tests/test_strip_box_suffix.py | 8 | 车型/厢型/尾板后缀规范化纯函数 |
| tests/test_mysql_cursor_adapter.py | 4 | MySQL 游标适配层 |
| tests/test_invoice_finance_access.py | 4 | 发票财务端权限 |
| tests/test_initial_payment_rules.py | 4 | 首付款/定金规则 |
| tests/test_role_dashboard.py | 2 | 角色工作台数据口径 |
| tests/test_receiving_company_finance_access.py | 2 | 收款公司财务可见性 |
| tests/test_model_guidance_price_mysql_migration.py | 2 | 指导价 MySQL 迁移 |
| tests/test_guidance_board.py | 2 | 指导价看板 |
| tests/test_legacy_contract_migration.py | 1 | 历史合同迁移 |
| **合计** | **86** | |

对比基线（70 例）净增 16 例，全部来自本计划 3.2 节扩展清单，无删减、无跳过。

---

## 三、接口扩展清单执行明细（计划 3.2 节 API-01～16）

新增用例文件：tests/test_plan_20260916_api.py（16/16 通过）。

| 编号 | 场景 | 关键断言（实测结果） | 优先级 | 结论 |
| --- | --- | --- | --- | --- |
| API-01 | 报单锁定不设期限 | 历史"待提交"草稿无 expires_at 过期逻辑（expire_sales_order_drafts 为空操作），跨天仍可编辑提交 | P1 | 通过 |
| API-02 | 多车（SKU）报单 | vehicle_ids 批量报单成功；部分车辆已锁定时整单 400 冲突；出库批量更新车辆状态 | P1 | 通过 |
| API-03 | 黑名单客户报单 | 提示文案含黑名单原因+标记时间；订单可创建；提交后自动转"待老板审批" | P0 | 通过 |
| API-04 | 取消订单边界 | 他人名下 403；重复发起 400；退款金额不等于应退定金 400；流水号缺失/过短 400；已出库 400 拦截；完成后车辆解锁回在库、订单已作废 | P0 | 通过 |
| API-05 | 续租全规则 | 到期后发起 400；起租日篡改 400；缺续租合同/编号 400；押金不足未补足 400；跨期挂"续租审批中"且车辆保持"租赁中" | P0 | 通过 |
| API-06 | 再次续租 | 同一合同二次续租激活后生成第三段租期，不新建合同主档 | P1 | 通过 |
| API-07 | 短租按天续租 | 按天续租跨月金额正确（1 月 96.77/天、2 月 107.14/天，按月租÷当月实际天数） | P1 | 通过 |
| API-08 | 退车/维修照片必填 | 退车缺外观/里程/工具任一照 400；维修完工缺三照 400 | P0 | 通过 |
| API-09 | 整备状态机 | 待整备、整备中、整备完成顺序流转；逆向/跳级 400；车辆状态联动（可租/可售） | P1 | 通过 |
| API-10 | 提前退车折算 | 月租÷当月实际天数×实际使用天数；2 月 28 天、31 天整月、跨月起算边界均正确 | P0 | 通过 |
| API-11 | 发票作废/红冲/驳回 | 已开票、作废、红冲流转正确；驳回原因必填（空原因 400） | P1 | 通过（执行中发现并修复 1 个 500 缺陷，见缺陷表 D-01） |
| API-12 | Excel 导入入库 | 模板列解析正确；重复 VIN 跳过并计数；非法行可追踪 | P1 | 通过 |
| API-13 | 指导价导入 | /api/model-guidance-prices/import 批量覆盖更新、历史留痕 | P2 | 通过 |
| API-14 | 销售数据范围 | 台账/应收/客户库/催收：销售（含自建第二销售 sales2）只见自己名下；老板/财务全量 | P0 | 通过 |
| API-15 | 隐藏字段脱敏 | vehicles 与 contracts 接口按角色脱敏敏感字段（采购价等），老板可见全量 | P1 | 通过 |
| API-16 | 幂等与重复提交 | 重复出库/重复核销/重复激活/重复退款均 400 拦截，不产生脏数据 | P0 | 通过 |

计划中原标注为测试空白的 8 项（API-02、04、06、07、09、11、12、13、15）本轮全部补齐。

---

## 四、L3 接口冒烟（六条核心流程，58/58）

详见《20260916-六条核心流程冒烟测试报告》。本轮复核结论维持：车辆入库 6/6、租赁出库 16/16、以租代售出库 13/13、对账 8/8、发票 8/8、指导价维护 7/7，公共登录 1/1。冒烟脚本 /private/tmp/jiefang_smoke.py，独立临时库，五角色默认账号。

---

## 五、L4 前端 E2E（Playwright 首批落地，8/8）

计划 3.3 节要求引入 Playwright。本轮完成环境搭建（Playwright 1.60.0 + Chromium）并执行首批用例，覆盖 UI-01 全部与 UI-07 部分。脚本 /private/tmp/jiefang_e2e_ui.py，冒烟环境 127.0.0.1:49170 + 全新临时库，截图证据存档 /private/tmp/jiefang-e2e/。

| 编号 | 用例 | 实测结果 | 结论 |
| --- | --- | --- | --- |
| UI-01a | 登录页渲染 | 登录表单与"金聚源车辆管理系统"标题正常渲染 | 通过 |
| UI-01b | 老板菜单矩阵 | 13 项菜单与 role_pages 种子完全一致（仪表盘/指导价看板/销售报单/客户库/车辆管理/历史完结/审批中心/账单管理/收款公司/对账单/锁车管理/利润核算/系统设置） | 通过 |
| UI-01b | 运营菜单矩阵 | 9 项一致（含发票申请，无客户库/账单/利润/设置） | 通过 |
| UI-01b | 财务菜单矩阵 | 11 项一致（无发票申请/锁车管理/系统设置） | 通过 |
| UI-01b | 车管菜单矩阵 | 5 项一致（仅仪表盘/指导价看板/车辆管理/历史完结/审批中心） | 通过 |
| UI-01b | 销售菜单矩阵 | 8 项一致（含锁车管理，无账单/对账/发票/利润/设置） | 通过 |
| UI-07 | 老板移动端 375px | scrollWidth=375 = clientWidth，无横向滚动 | 通过 |
| UI-07 | 销售移动端 375px | scrollWidth=375 = clientWidth，无横向滚动 | 通过 |

说明：运营角色菜单渲染顺序（发票申请在对账单之前）与种子数组顺序不同，经核实为前端 menuItems 声明顺序使然，集合一致，不构成缺陷。UI-02～UI-06（报单表单交互、出库拦截、审批中心、筛选组合、黑名单提示）尚未自动化，列入后续迭代（见第十章）。

---

## 六、权限矩阵执行结果（计划第 4 章，P0）

脚本 /private/tmp/jiefang_permission_matrix.py：遍历路由表提取 require_role 装饰器生成静态规则，并对 P0 接口做五角色实测。

### 6.1 静态规则

共提取 **145 条 API 权限规则**（148 个路由中，auth/login、auth/logout、auth/me 为登录态公开接口；vehicles/import 为接口内手动鉴权）。

### 6.2 P0 接口 x 五角色实测（95/95 通过）

| 接口 | 老板 | 运营 | 财务 | 车管 | 销售 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 车辆手工入库 POST /api/vehicles | 允许 | 允许 | 403 | 403 | 403 | 通过（诉求 18：车管无入库） |
| 车辆出库 POST /api/vehicles/1/deliver | 允许 | 403 | 403 | 允许 | 403 | 通过 |
| 对账核销 POST /api/reconciliation/1/verify | 允许 | 403 | 允许 | 403 | 403 | 通过 |
| 发票申请 POST /api/contracts/1/invoices | 允许 | 允许 | 403 | 403 | 403 | 通过 |
| 发票开票 POST /api/invoice-requests/1/issue | 允许 | 403 | 允许 | 403 | 403 | 通过 |
| 发票作废 POST /api/invoice-requests/1/void | 允许 | 403 | 允许 | 403 | 403 | 通过 |
| 指导价维护 POST /api/model-guidance-prices | 允许 | 403 | 403 | 403 | 403 | 通过 |
| 指导价导入 POST /api/model-guidance-prices/import | 允许 | 403 | 403 | 403 | 403 | 通过 |
| 黑名单标记 POST /api/customer-blacklist | 允许 | 403 | 403 | 403 | 403 | 通过 |
| 销售报单 POST /api/sales-orders | 允许 | 403 | 403 | 403 | 允许 | 通过 |
| 取消订单发起 POST /api/order-refunds | 允许 | 403 | 403 | 403 | 允许 | 通过 |
| 取消订单审批 POST /api/order-refunds/1/approve | 允许 | 403 | 403 | 403 | 403 | 通过 |
| 取消订单退款 POST /api/order-refunds/1/pay | 允许 | 403 | 允许 | 403 | 403 | 通过 |
| 续租发起 POST /api/contracts/1/renewals | 允许 | 允许 | 403 | 403 | 允许 | 通过 |
| 续租激活 POST /api/renewals/1/activate | 允许 | 允许 | 403 | 403 | 403 | 通过 |
| 整备开始 POST /api/refurbishment-records/1/start | 允许 | 403 | 403 | 允许 | 403 | 通过 |
| 车管验车 POST /api/return-inspections/1/fleet | 允许 | 403 | 403 | 允许 | 403 | 通过 |
| 退车运营结算 POST /api/return-inspections/1/operator | 允许 | 允许 | 403 | 403 | 403 | 通过 |
| 报单财务确认 POST /api/sales-orders/1/activate | 允许 | 403 | 允许 | 403 | 403 | 通过 |

判定方式："允许"以穿透权限装饰器到达业务校验返回 400 为特征，"拒绝"为装饰器直接 403。

### 6.3 设计说明（非缺陷）

require_role 装饰器中对老板角色通配放行，对应诉求第 3 条"超管端与老板是同一人，不拆分权限"。矩阵中老板列"允许"均为此设计预期。

### 6.4 未登录访问抽检（10 个接口）

/api/vehicles、/api/sales-orders、/api/contracts、/api/customer-library、/api/reconciliation/list、/api/model-guidance-prices、/api/customer-blacklist 共 7 个接口未登录均 401，通过；**3 个接口未登录返回 200，已登记为缺陷 D-02**（见第九章）。

---

## 七、数据与迁移测试（计划第 5 章）

| 项 | 方法 | 结果 | 结论 |
| --- | --- | --- | --- |
| init_db 幂等 | 正式库副本（45 张表）连跑两轮 init_db 与 migrate_legacy_pending_approvals，对比各表行数 | 两轮行数变化 0，safe_alter 重复执行无报错 | 通过 |
| 副本可用性 | 副本库起 test_client 调核心列表接口 | 全部 200 | 通过 |
| MySQL 适配 | pytest 游标适配层与指导价迁移用例 | 6 例通过（test_mysql_cursor_adapter 4 例 + test_model_guidance_price_mysql_migration 2 例） | 通过 |
| SQLite 到 MySQL 全量对比 | 计划要求双库跑同一 L2 套件 | 未执行（需 MySQL 环境），列入后续 | 待办 |
| 备份恢复抽检 | 最近 3 个 .bak 库启动并调核心接口 | 未执行，列入后续 | 待办 |

---

## 八、非功能摸底（计划第 6 章）

### 8.1 性能基准（正式库副本实测）

数据量：车辆 790、合同 528、账单 10184、客户 296、审计日志 2690。Flask test_client 计时，每接口每角色 3 次取最小/最大值。

| 接口 | boss | sales | 标准（P95 小于 2s） | 结论 |
| --- | --- | --- | --- | --- |
| 对账列表 | 229-240ms | 17-23ms | 达标 | 通过 |
| 客户库 | 198-215ms | 13-20ms | 达标 | 通过 |
| 报单台账 | 19ms | 19-26ms | 达标 | 通过 |
| 车辆列表（分页 20） | 9-10ms | 9-11ms | 达标 | 通过 |
| 车辆精简列表 | 24-25ms | 27-29ms | 达标 | 通过 |
| 合同列表 | 75-80ms | 6-7ms | 达标 | 通过 |
| 发票列表 | 4-5ms | 403（无权限，符合预期） | 达标 | 通过 |
| 退车单列表 | 3ms | 3ms | 达标 | 通过 |
| 指导价看板 | 5-6ms | 5-6ms | 达标 | 通过 |
| 工作台统计 /api/dashboard/stats | **5777-6068ms** | **5758-5816ms** | **超标约 3 倍** | 不通过（登记 D-03） |
| 角色工作台 /api/dashboard/workspace | **5801-5859ms** | 428-438ms | 老板角色超标 | 不通过（登记 D-03） |

初步定位：两个接口每次请求都先执行 check_overdue() 全量逾期扫描（1 万+ 账单与合同联表），随后 build_dashboard_metrics 做多轮全表聚合；数据量增长后将继续线性恶化。建议：逾期扫描改定时任务或按需触发、聚合结果缓存。登记为性能缺陷（严重级：一般，不阻断本期发版，但需排期优化）。

### 8.2 安全基线

| 项 | 结果 |
| --- | --- |
| 未登录访问受保护 API | 7/10 抽检 401，3 个接口未鉴权（D-02） |
| 密码存储 | users 表密码明文存储，登记 D-04，建议 bcrypt/argon2 哈希 |
| 角色越权（P0 接口实测） | 未见越权（95/95） |
| 上传路径穿越/超大文件/非法类型 | 未执行专项，列入后续 |
| session 过期策略 | 未执行专项，列入后续 |

### 8.3 并发幂等

L2 层 API-16 已验证重复出库/核销/激活/退款的业务级幂等拦截（400）。真实并发（双请求同时写）受 SQLite 串行写保护，本期风险低，按计划在 MySQL 迁移后必测。

---

## 九、缺陷登记表

| 编号 | 缺陷 | 严重级 | 状态 | 说明 |
| --- | --- | --- | --- | --- |
| D-01 | 老板驳回发票申请时若未填驳回原因，接口 500（boss_reject_invoice_request 空原因分支误调未赋值的 conn.close()） | 一般 | **已修复**（删除误调用，app.py 约 7466 行），API-11 回归通过 | 本轮新发现，修复前 HEAD 即存在 |
| D-02 | 3 个接口无任何鉴权装饰器，未登录返回 200：GET /api/audit-logs、GET /api/return-inspections、GET /api/contracts/<cid>/approval-status | 中低危（只读，但 audit-logs 含操作审计信息） | **已修复**（第二轮，见第十节） | 两个只读接口补 @login_required；audit-logs 收紧为 @require_role('老板') |
| D-03 | 工作台接口性能超标：/api/dashboard/stats 各角色约 5.8s，/api/dashboard/workspace 老板约 5.8s（标准 P95 小于 2s） | 一般（体验型） | **已修复**（第二轮，见第十节） | 真实根因为月度图表循环内 4.4 万次重复 strftime；预计算后接口降至约 0.6s |
| D-04 | users 表密码明文存储 | 中危（安全基线登记项） | **已修复**（第二轮，见第十节） | pbkdf2:sha256:260000 哈希；存量明文启动时自动迁移，登录成功即升级 |

---

## 十、缺陷修复复核（2026-09-16 第二轮）

针对第一轮登记的 D-02、D-03、D-04 完成修复并复核，全部通过；86 例自动化回归全量重跑 86/86 通过。

| 编号 | 修复内容 | 复核结果 |
| --- | --- | --- |
| D-02 | /api/return-inspections 与 /api/contracts/<cid>/approval-status 增加 @login_required；/api/audit-logs 增加 @require_role('老板')（前端与测试均无该接口消费方，收紧为老板专属） | 未登录访问 3 个接口均 401；老板访问 audit-logs 200，销售 403；销售访问 return-inspections 200（业务需要） |
| D-03 | cProfile 定位真实根因：build_dashboard_metrics 月度图表在 12 个月循环内对每行重复执行 normalize_date 与 end.strftime（共 4.4 万余次 strftime，占 5.3s），SQL 查询本身均为毫秒级。修复为循环前预计算"金额+对比日期"元组、月末字符串提升出循环，语义不变 | 正式库副本实测：build_dashboard_metrics 6.01s 降至 0.04s；/api/dashboard/stats 约 0.59s；/api/dashboard/workspace（老板）约 0.57s，均达到 P95 小于 2s 标准；剩余耗时主要为 check_overdue 逾期重算（约 0.5s） |
| D-04 | 登录改为按用户名查询后 check_password_hash 校验（pbkdf2:sha256:260000）；历史明文密码登录成功即原地升级为哈希；database.py 新增 migrate_plaintext_passwords，seed 启动时幂等迁移全部存量明文 | 明文账号首登 200 且密码列即时哈希化；哈希后再次登录 200；错误密码 401；全量迁移后明文密码剩余 0；测试直接 INSERT 的明文用户经兼容路径仍可登录 |

---

## 十一、计划覆盖度与后续安排

| 计划项 | 状态 |
| --- | --- |
| 3.2 接口扩展 API-01～16 | 已完成 16/16 |
| 3.3 UI-01、UI-07（部分） | 已完成首批 8 条 |
| 3.3 UI-02～UI-06（报单表单/出库/审批中心/筛选/黑名单提示） | 待办（下一迭代） |
| 4 权限矩阵脚本化与 P0 实测 | 已完成（145 规则 + 95 实测点） |
| 5 迁移幂等、MySQL 适配用例 | 已完成；双库全量对比、备份恢复抽检待办 |
| 6 性能/安全摸底 | 已完成摸底并登记缺陷；并发实测、上传专项待办 |
| 11 交付物（用例集/E2E 脚本/权限矩阵/冒烟报告/验证报告/本报告） | 已齐；E2E 脚本暂存 /private/tmp，建议迁入 tests/e2e/ 并纳入发版检查 |

准出核对（计划第 9 章）：P0/P1 用例 100% 通过；六流程冒烟 100% 通过；权限矩阵无越权；阻断/严重缺陷为 0（D-01 已修复，D-02/03/04 均为一般或登记级）。

**结论：本期测试目标达成，系统具备发版条件。建议发版前完成正式库备份，发版后按生产巡检流程抽查一笔真实租赁流程。**

---

## 附录：执行产物索引

| 产物 | 位置 |
| --- | --- |
| 自动化用例（86 例） | tests/（新增 tests/test_plan_20260916_api.py 16 例） |
| 六流程冒烟脚本与结果 | /private/tmp/jiefang_smoke.py、/private/tmp/jiefang-smoke-results.json |
| E2E 脚本与截图 | /private/tmp/jiefang_e2e_ui.py、/private/tmp/jiefang-e2e/（results.json 与 8 张截图） |
| 权限矩阵脚本与结果 | /private/tmp/jiefang_permission_matrix.py、/private/tmp/jiefang-permission-matrix.json |
| 迁移幂等脚本 | /private/tmp/jiefang_migration_check.py |
| 性能摸底脚本 | /private/tmp/jiefang_perf_baseline.py |
| 关联报告 | 《20260916-8.25诉求第二版全量验证报告》《20260916-六条核心流程冒烟测试报告》《20260916-测试计划方案》 |

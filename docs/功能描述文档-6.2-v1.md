# 金聚源租车管理系统 · 功能描述文档(6.2-v1)

> **版本**:6.2-v1(2026-06-02,即 v3.1-v2 的发布命名)
> **基线**:[流程图修改5.27.md](流程图修改5.27.md)
> **上一版**:[功能描述文档-5.30.md](功能描述文档-5.30.md)(v3.1)
> **本次修订依据**:[功能描述文档-5.30-Review报告.md](功能描述文档-5.30-Review报告.md) + 业务方逐条确认意见(锁车/解锁审批流、H1 触发与临近还款判定、退车流程确认、滞纳金逾期首日起算、状态字典与建表数订正等)
>
> **阅读方式**:每个功能项按 **【模块】→【功能】→【触发】→【输入】→【处理规则】→【输出/状态变化】→【异常】→【界面要点】** 的统一结构描述。
>
> **v3 关键变化(详见末尾"v3 变更日志")**:
> ① F3 比对维度 2、维度 3 均改为展示项不参与判定,唯一判定项是"利差区间校验"(客户分期总额 − 厂家分期总额 落在预期毛利区间);② H1 扫描清单更新到逐日 `逾期N日` 状态、`check_overdue` 重写为逐日状态机,并新增 `未激活` 隔离激活前窗口;③ H3 重写——**首付款与押金一律不可抵扣**,逾期仅按客户真实还款递减,租赁与以租代售处理一致;④ F3 客户/厂家期数差以**月度现金流对齐**落定(与模块 P 共用),累计净流水 = 客户分期总额 − 厂家分期总额 = 单车毛利;⑤ G2 财务侧补 contracts 写回说明,状态值统一为"已通过";⑥ N1 触发条件加 `contract_type='销售'` 守卫,租赁走 M;G3 出库门禁按业务模式区分;⑦ 发票开票主体与合同主体解耦,按申请填写开票;⑧ N1 结清前关闭审核中减免;⑨ vehicle_rebates 去掉 visibility 冗余字段,统一走 U2 矩阵;⑩ 附录 W 补 role_pages/role_actions 建表;⑪ waivers 增 `(contract_id,waiver_kind,status)` 复合索引;⑫ 决策项计数订正为 1~21、23~36 共 35 项,清理变更日志陈旧措辞。

---

## 目录

- [模块 A — 用户与权限](#模块-a-用户与权限)
- [模块 B — 车辆资产管理](#模块-b-车辆资产管理)
- [模块 C — 指导价管理(双价 + 版本)](#模块-c-指导价管理双价--版本)
- [模块 D — 销售报单(租赁 / 以租代售)](#模块-d-销售报单租赁--以租代售)
- [模块 E — 价格特批](#模块-e-价格特批)
- [模块 F — 还款计划(双表生成与比对)](#模块-f-还款计划双表生成与比对)
- [模块 G — 合同与出库](#模块-g-合同与出库)
- [模块 H — 每日监控与催收](#模块-h-每日监控与催收)
- [模块 I — 还款确认与计划表调整](#模块-i-还款确认与计划表调整)
- [模块 J — 滞纳金](#模块-j-滞纳金)
- [模块 K — 租金减免](#模块-k-租金减免)
- [模块 L — 发票申请](#模块-l-发票申请)
- [模块 L-附 — 对账与上传规则(全局)](#模块-l-附--对账与上传规则全局)
- [模块 M — 退车(到期 / 提前)](#模块-m-退车到期--提前)
- [模块 N — 过户(以租代售结清 / 提前结清)](#模块-n-过户以租代售结清--提前结清)
- [模块 O — 车辆返利](#模块-o-车辆返利)
- [模块 P — 单车盈利统计](#模块-p-单车盈利统计)
- [模块 Q — 老板穿透看板](#模块-q-老板穿透看板)
- [模块 R — 收款公司主数据](#模块-r--收款公司主数据)
- [模块 S — 客户黑名单](#模块-s--客户黑名单)
- [模块 U — 字段级权限矩阵](#模块-u--字段级权限矩阵)
- [模块 V — 非功能要求(幂等 / SLA / 时间口径)](#模块-v--非功能要求幂等--sla--时间口径)
- [附录:状态字典](#附录状态字典)

---
## 模块 A — 用户与权限

### A1 登录 / 登出
- **触发**:用户访问任意非 `/login` 路由。
- **输入**:用户名、密码。
- **规则**:命中 `users.is_active=1` 才能登录;失败仅返回"账号或密码错误",不区分。
- **输出**:写入 session,返回 `display_name`、`role`、`role_pages`、`role_actions`。
- **异常**:连续 5 次错误锁定 10 分钟(可选做)。

### A2 角色矩阵(对照 5.27 流程,v2 修订)

| 角色 | 工作台主功能 | 关键不可见数据 |
|---|---|---|
| 销售 | 报单、催款 T+7、减免申请(租金/滞纳金)、退车发起、提前退车/结清、报价单查看 | 采购价、税率、返利、毛利/净利、单车盈利 |
| 财务 | 报单确认、厂家表上传与比对、首款流水回填、还款核销、减免复核执行、发票开具/作废/重开、返利录入、退车押金、提前结清收款 | — |
| 运营 | 合同 PDF 上传、催款 T+3、本期还款上传、发票申请、过户登记、提前结清本金计算、多还抵扣月份与客户确认 | 采购价、税率、返利、毛利/净利 |
| 车管 | 入库、出库、验车 | 价格类全部 |
| 老板 | 双指导价维护、价格特批、退车领导审批、租金/滞纳金减免审批、全过程穿透、返利全量可见 | — |

> **决策项 32**:**销售视角永不展示盈利/采购价/返利**,无论合同是否已结清。盈利数据仅财务、老板可见(详见模块 P、U)。

### A3 操作审计
- **触发**:任何状态变更类接口被调用。
- **规则**:写 `audit_logs(action, target_type, target_id, detail, operator, ip_address)`。
- **不能跳过的动作清单**:报单创建/审批/驳回/作废、合同生成、计划比对、还款核销、减免申请/审批/执行/撤销、发票开具/作废/重开、退车节点、过户、出库、返利、指导价维护、黑名单维护(详见模块 S)、字段级权限变更。

### A4 字段级权限
- 见模块 U(字段级权限矩阵),由前端组件按角色 + 字段动态渲染,后端在 DTO 层做兜底过滤。

---

## 模块 B — 车辆资产管理

### B1 车辆入库
- **触发**:车管端"新增车辆"。
- **输入**:VIN(17 位,必填唯一)、车牌、车型、车厢、电池、是否新车、开票价、采购价、税率、月折旧期数、保险到期、年检到期、附件(发票/合同 PDF)。
- **规则**:
  1. VIN 长度=17,必须大写;重复返回"该 VIN 已入库"。
  2. `status` 初始化为 `在库`,`activated_at=NULL`(新车)或当前(二手)。
  3. 系统按"开票价 × 残值率"自动算 `estimated_residual_value`。
  4. 若车型在 `model_guidance_prices` 已维护双价,则不要求录入单车指导价。
- **输出**:车辆主数据 + 入库审计日志。
- **异常**:VIN 长度错;车型主数据缺失时仅警告,不阻塞。
- **界面要点**:车辆入库后即可在销售端"在库车辆下拉"中选择。

### B2 车辆状态机(v2 修订,决策项 6)

```
在库 ──报单创建──> 报单锁定中 ──报单驳回/作废──> 在库
报单锁定中 ──出库(租赁)──> 租赁中
报单锁定中 ──出库(以租代售)──> 以租代售
租赁中 ──退车发起(M1)──> 退车中 ──车管验车(M2)──> 待维修(若验车判定需修) / 在库(若无问题)
以租代售 ──结清+过户──> 已售/已过户
以租代售 ──提前结清+过户──> 已售/已过户
待维修 ──维修完成──> 在库
租赁中 / 以租代售 ──事故报修(运营登记,合同存续期内)──> 维修中 ──修复──> 租赁中 / 以租代售
```

> **修订要点**:
> - **删除**了 v1 中"租赁中/以租代售 ──维修──> 待维修"的直接路径。租赁/在售期间发生事故仅切换为 `维修中`(临时维修态,不影响合同),修复后回到原营业态。
> - **`待维修` 仅作为退车验车后的中转态**,由 M2 验车结果决定。
> - 新增 `退车中` 显式状态,贯穿 M1~M5,M6 出款完成后才落库。

### B3 车辆查询
- **角色**:全员可见列表(含老板穿透),不同角色看到的字段集不同(详见模块 U)。销售不见采购价、税率、返利、盈利相关。
- **筛选**:VIN、车牌、车型、状态、公司、车厢、是否新车。
- **导出**:Excel(老板/财务)。

---

## 模块 C — 指导价管理(双价 + 版本)

### C1 维护"租赁每期分期指导价"
- **触发**:老板端 `/admin/guidance-prices/lease`。
- **输入**:车型、租赁分期指导价(元/期)、备注、生效时间(默认即时)。
- **规则**:
  1. 写 `model_guidance_prices.lease_installment_price`,旧值入 `model_guidance_price_history(price_kind='lease_installment')`。
  2. **存量订单不变价**:已生成的报单/合同上的双价快照锁定在"报单提交时刻"。
  3. **新增报单按最新价**:任何"待价格特批驳回 → 重新报单"也按调整后最新价。
- **输出**:历史记录 + 影响车辆数(同车型在库数)。
- **异常**:价格 ≤ 0 不允许保存。
- **界面要点**:旁边显示该车型当前在库数、在租数。

### C2 维护"以租代售整车指导价"
- 与 C1 一致,字段=`sale_total_price`,`price_kind='sale_total'`。

### C3 指导价缺失硬性阻塞(决策项 9)
- **触发**:销售端进入"新建报单"或保存草稿。
- **规则**:
  - 若所选 VIN 对应车型的双指导价**任一为空或 ≤ 0**,**禁止提交报单**(草稿可保存但不能进入审批流),前端弹窗提示"请联系老板维护车型指导价"。
  - 老板工作台首页新增"指导价缺失"红色提醒卡片,列出在库且车型缺指导价的车辆数,点击直达 C1/C2 维护页。
- **输出**:阻断报单流程,保留草稿。
- **异常**:历史数据(在租中已无指导价)不阻塞,仅在老板看板提醒。

---

## 模块 D — 销售报单(租赁 / 以租代售)

### D1 报单状态字典(v2 简化,决策项 8、24)

| 状态 | 含义 | 说明 |
|---|---|---|
| `草稿` | 销售保存未提交(决策项 10) | 可反复编辑、删除;不锁车 |
| `待价格特批` | 报价低于指导价,等待老板 | **锁车** |
| `待财务确认` | 价格通过,等待财务比对计划表与确认 | **锁车** |
| `已激活` | 财务确认完成,合同/计划落库 | **锁车** |
| `已作废` | 终止态(驳回、撤回、作废统一收口) | **释放车辆** |

> **决策项 8**:删除原"价格特批驳回""财务驳回"两个独立状态,合并到 `已作废`(操作日志内区分原因)。
>
> **决策项 17**:**老板特批驳回 = 直接置 `已作废`,不允许在原报单上重提**;销售如需修改后再报,通过"以草稿复制"生成新报单。

### D2 草稿保存与失效(决策项 10、26)
- **触发**:销售端"暂存草稿"按钮。
- **规则**:
  1. 草稿允许多次编辑、删除,**不锁车**(避免一辆车被多个草稿同时占用)。
  2. 草稿生效期 7 天,过期自动作废(不影响库存)。
  3. **快照规则**:草稿不写入双价快照,仅在"提交报单"瞬间从 `model_guidance_prices` 读取最新值并写入 `sales_orders.snapshot_*`,确保快照永远反映"提交时点的最新指导价"。
  4. 若草稿对应车辆在保存期间已被其他报单锁定,提交时报错"该车辆已被其他报单占用",草稿保留供调整 VIN。
- **输出**:`sales_orders.order_status='草稿'`,`saved_at`、`expires_at` 落库。

### D3 新建报单(提交)
- **触发**:草稿点"提交报单"或直接"新建并提交"。
- **输入**:
  - 共同字段:VIN、客户、客户电话、收款公司(模块 R)、起租日、销售方式(租赁/以租代售)、销售顾问、备注、附赠物。
  - **租赁**:租期、月租金、押金、首期付款日。
  - **以租代售**:分期数、首付款、月租金、整车成交价。
- **规则**:
  1. 仅 `vehicles.status='在库'` 可报。
  2. 一辆车同时只能存在一个进行中的报单(状态 ∈ {待价格特批、待财务确认、已激活})。
  3. **报单提交即锁车**:`vehicles.status='报单锁定中'`。
  4. **客户黑名单校验**:命中模块 S 黑名单 → 直接拒绝提交,提示销售联系老板审核解禁。
  5. **指导价校验**(决策项 9):双价任一缺失直接拒绝。
  6. **价格核对**:
     - 租赁:`rent < lease_installment_price` ⇒ 触发特批
     - 以租代售:`sale_total_price < sale_total_price_guidance` ⇒ 触发特批
  7. 写入 `sales_orders` 并保存双价快照 `snapshot_lease_installment_price`、`snapshot_sale_total_price`(从最新指导价读取,见 D2)。
- **输出**:`order_status='待价格特批'` 或 `'待财务确认'`。
- **异常**:VIN 不存在 / 车不在库 / 已有进行中报单 / 黑名单命中 / 指导价缺失。
- **界面要点**:右侧实时显示当前两条指导价及差额预警条;客户被加入黑名单时弹窗提示。

### D4 报单作废
- **触发**:① 老板特批驳回(决策项 17);② 财务确认时发现严重问题(改为驳回);③ 销售主动撤回(状态=待价格特批/待财务确认时)。
- **规则**:
  1. 报单状态置 `已作废`,**车辆状态恢复 `在库`**。
  2. 销售可"复制"该报单到草稿,改价后重新提交(等同新报单)。
  3. 审计日志记录作废原因(驳回原因/撤回原因)。
- **输出**:`sales_orders.voided_at`、`void_reason`、`voided_by` 落库。

### D5 报单查询
- **筛选**:状态、销售方式、销售顾问、客户、时间区间。
- **批量动作**:仅老板可批量驳回(=作废);财务可批量确认(同状态)。

---

## 模块 E — 价格特批

### E1 老板特批
- **触发**:报单 `order_status='待价格特批'`。
- **输入**:通过 / 驳回 + 备注(驳回必填)。
- **规则**:
  1. 命中 `approval_flows.ref_type='price_exception'`,只一步,角色=老板。
  2. 通过后报单转 `待财务确认`,**车辆继续锁定**。
  3. 驳回直接进入 D4 作废流程(决策项 17)。
- **输出**:更新 `boss_price_approved_by/at`、`price_check_status='已通过'`。
- **SLA**(决策项 20):老板特批 24h 未处理 → 系统每日给老板发提醒;销售可主动催办,催办记录写 audit_logs。

### E2 财务确认报单(F→E 时序解耦,决策项 1)
- **触发**:`order_status='待财务确认'`。
- **前置**:必须先完成 F1(系统生成客户计划)、F2(财务上传厂家表)、F3(双表比对)且 `customer_plan_match_status='已通过'` 或"差异已确认"。
- **规则**:
  1. 财务核对客户计划与厂家计划比对结果,确认无误。
  2. 通过后 `order_status='已激活'`,触发 P 模块"单车盈利基础数据"生成,允许进入合同生成与还款计划落库。
  3. **驳回**:走 D4 作废流程,审计原因。
- **异常**:比对失败时不允许激活,前端提示具体差异列表(详见 F3)。
- **界面要点**:确认按钮旁显示"客户计划期数 vs 厂家计划期数 vs 比对状态"三段式徽章。

---


## 模块 F — 还款计划(双表生成与比对)

> **决策项 1(关键)**:F、E 时序在 v2 解耦。客户计划由"已激活"前置生成,即:**草稿/特批通过 → 待财务确认 → 系统生成客户表 → 财务上传厂家表 → 双表比对 → 比对通过 → 财务点击"激活报单" → `已激活`**。F 不再依赖 E2 完成,反而是 E2 的前置。

### F1 系统生成"客户还款计划表"
- **触发**:报单进入 `待财务确认`(由 E1 通过或非特批直接进入)后,系统自动写 `repayments`(客户计划专用表;v3 修订:v2 提案的 `plan_kind` 字段冗余,客户/厂家计划走双表分离设计——客户表=`repayments`、厂家表=`factory_repayments`,不再用单字段区分)。
- **落库状态(v3 新增,②)**:F1 生成的每期 `repayments.status` 一律落 `未激活`,**不进入 H1 日终监控**。只有 F4 经 E2 激活后才整表改 `待还款`。
  > **② 修订原因**:v2 在 F1 落库即给 `待还款`,而报单尚未经 E2 激活、合同未签、车未出库,H1 却已开始按 due_date 计提滞纳金、推催款——对一份还没生效的计划误催误罚。新增 `未激活` 中间态隔离这段窗口。
- **计划表结构(v3 明确,⑥)**:
  - **以租代售(`contract_type='销售'`)**:`首付款` 行 + `第一期` + `第二期` … + `第 loan_periods 期`。
    - **首付款是独立行,不并入第一期还款**;首付款支付(G2 财务审批通过)后才允许出库(G3)。
    - 第一期及以后纯为月供(`rent`),不含首付款。
  - **租赁(`contract_type='租赁'`)**:`押金` 行 + `第一期` + `第二期` … 。
    - 押金 + 第一期租金支付后,车辆才出库(G3);第一期租金可在 G2 财务审批时自动核销(见 G2 写回)。
- **规则**:
  - **租赁**(决策项 14):
    - 第 1 期 `due_date = 起租日`(首期 + 押金已收,通常起租当日)。
    - 第 N 期 `due_date = 第 1 期 due_date + (N-1) × 月`。**租赁是否首期当月收取以合同条款为准**;若合同标记"首期延后",则按"次月起还"模式。
  - **以租代售**:`首付款` 行无 due_date 计息属性(出库前置,非分期);第 1 期 `due_date = 起租日 + 1 月`(次月起还);共 `loan_periods` 期月供。
  - 金额=`rent`(每期月租)。
- **重算**:若财务在 E2 之前发现客户输入有误,要求销售改单 → 销售改单后 F1 自动重算并清空 F2、F3 结果,需要重新上传厂家表。

### F2 财务上传"厂家还款计划表"
- **触发**:F1 完成后,财务端"上传厂家计划表"按钮亮起。
- **输入**:Excel(沿用现有模板,识别"应还款日期/客户应还金额合计")。
- **规则**:解析后写 `factory_repayments(period, due_date, amount)`,期数自增。
- **异常**:模板表头识别失败提示"未识别到厂家还款计划表头"。

### F3 系统比对(v3 修订:维度 2 改为展示项,决策项 11)
- **触发**:F2 完成后自动触发,或财务点"重新比对"。
- **比对维度(v3 修订)**:
  1. **利差区间校验(判定项,v3.1-v2 修订)**:`spread_total = Σ customer.amount − Σ factory.amount`,要求 `spread_total` 落在**预期利差/毛利区间 `[下限, 上限]`**(可配置)。**注意:客户总额本就应当大于厂家总额,差出来的就是单车毛利,二者并非"应当相等"。** 旧版"总额≈一致、允许差额ε"的口径会把正常毛利误判为差异,本版废止,改为"差额落在预期毛利区间即通过"。
  2. **单期均值参考(仅展示,不参与判定)**:
     ```
     factory_avg  = Σ factory.amount  / factory_periods       # 厂家单期均值
     customer_avg = Σ customer.amount / customer_periods      # 客户单期均值
     spread       = customer_avg - factory_avg                # 利差/管理费/期(展示)
     ```
     展示该 spread 供财务判断利差是否合理。**v2 原"归一化单期金额"公式数学上前后抵消,且与维度 1"允许利差"前提自相矛盾——存在利差必报差异——v3 改为仅展示参考,不再纳入判定,避免误报**。
  3. **首末期日期参考(仅展示,不参与判定,v3.1-v2 修订)**:展示 `factory[0].due_date`/`customer[0].due_date` 与双方末期日期,供财务判断起止区间是否合理。**注意:客户分期与厂家分期的期数、起止月天然不同(如厂家 1–12 月、客户 3–8 月),首末期日期本就对不上,不应作为判定条件。** 旧版"首末期日期必须相等"的判定口径会把正常的月度现金流方案误判为差异,本版废止,降级为展示参考。
- **判定结果**:维度 1 与维度 3 同时通过 ⇒ `customer_plan_match_status='已通过'`;否则 `差异待处理`。差异行高亮,允许财务点"以厂家表为准重算客户表"或"以系统客户表为准忽略差异(填写差异说明)" ⇒ `差异已确认`。
- **输出**:`customer_plan_match_status ∈ {已通过, 差异待处理, 差异已确认}` + 差异行明细。
- **决策项 11**:差异说明纳入审计,通过/差异已确认后可进入 E2。
- **⑤ 客户期数与厂家期数不一致——v3.1-v2 已定方案:月度现金流时间轴对齐(不做逐期勾稽)**:
  - **现象**:客户分期(如 6/24 期)与厂家分期(如 12/36 期)期数、起止月天然不同,**无法逐期 due_date 一一对应**。
  - **解决思路(业务方明确口径)**:**不强行做期数映射 / 插值**,改为把厂家分期(资金流出,负)与客户分期(资金流入,正)**全部摊到同一条自然月时间轴**上,逐月叠加算"每月净流水 + 累计净流水"。两套分期不必同期数、不必同起点,只要落在同一时间轴即可对齐。
  - **示例**(车 a:厂家 12 期×-10000,起 1 月;客户 6 期×+25000,起 3 月):

    | 月份 | 厂家流出(期) | 厂家额 | 客户流入(期) | 客户额 | 每月净流水 | 累计净流水 |
    |---|---|---|---|---|---|---|
    | 1 月 | 1 | -10000 | — | — | -10000 | -10000 |
    | 2 月 | 2 | -10000 | — | — | -10000 | -20000 |
    | 3 月 | 3 | -10000 | 1 | +25000 | +15000 | -5000 |
    | 4 月 | 4 | -10000 | 2 | +25000 | +15000 | +10000 |
    | 5 月 | 5 | -10000 | 3 | +25000 | +15000 | +25000 |
    | 6 月 | 6 | -10000 | 4 | +25000 | +15000 | +40000 |
    | 7 月 | 7 | -10000 | 5 | +25000 | +15000 | +55000 |
    | 8 月 | 8 | -10000 | 6 | +25000 | +15000 | +70000 |
    | 9 月 | 9 | -10000 | — | — | -10000 | +60000 |
    | 10 月 | 10 | -10000 | — | — | -10000 | +50000 |
    | 11 月 | 11 | -10000 | — | — | -10000 | +40000 |
    | 12 月 | 12 | -10000 | — | — | -10000 | +30000 |

  - **校验口径**:末月累计净流水 = `Σ客户回款 − Σ厂家支出` = 150000 − 120000 = **+30000 = 单车毛利**,与维度 1(总额)结论自洽。
  - **F3 比对据此简化**:维度 1(总额 ε 容差)+ 维度 3(首末期落月)通过即放行;**期数差异不再报错**,中间期次以"月度现金流叠加表"展示给财务,不做行级勾稽判定。
  - **落地**:该月度时间轴即模块 P 单车盈利的核心算法(见 P1),F3 与 P 共用同一张"月度现金流表"。

### F4 进入财务确认
- **触发**:F3 通过/差异已确认。
- **规则**:进入 E2;E2 通过后报单 `已激活`,系统同时把 F1 落库的 `repayments`(此前为 `未激活`)整表激活为 `status='待还款'`(②)并触发 P 模块。**激活前的 `未激活` 期次不被 H1 扫描、不计滞纳金、不推催款**。

---

## 模块 G — 合同与出库

### G1 线下签合同 + 上传 PDF
- **触发**:E2 已激活后销售线下签字。
- **输入**:合同 PDF(必填)、合同编号、起止日。
- **规则**:写 `contracts` 主记录,`delivery_status='待出库'`。
- **决策项 4**:**合同表不再保留 `snapshot_guidance_price` / `snapshot_invoice_price` 双指导价快照**(冗余且与 `sales_orders` 重复)。指导价快照只在报单层 `sales_orders` 落库,合同读取走外键关联,避免双写不一致。
- **输出**:`contract_status='执行中'`。

### G2 客户支付首款(决策项 3 修订;v3 补"contracts 写回")
- **触发**:客户线下打款。
- **运营**(对客户):登记 → `contract_initial_payments(payment_type='首付款'/'押金+首期', status='待审批')`。
  - **必填**:实付金额、**客户付款截图**(运营对客户面,截图为关键凭证)。
- **财务**(对公司):对账 → `contract_initial_payments.status='已通过'`(代码现取值,v2 文档原写"已审批",v3 与代码对齐)。
  - **必填**:银行流水号、到账金额。
  - **选填**:公司收款截图。
- **规则**:租赁=押金+首期月租金;以租代售=首付款。
- **决策项 3 原则**:**运营/销售面对客户的环节,客户截图必填;财务对账以流水号为唯一锚点必填,公司截图选填**(详见 L-附)。
- **v3 新增 — contracts 同步写回(财务审批通过时由系统自动执行)**:
  - 若有押金 → `contracts.deposit_status='已收'`、`contracts.collected_deposit=押金金额`。
  - 若有首付/合同类型为销售 → `contracts.down_payment_status='已收'`。
  - 一律 → `contracts.delivery_status='待出库'`。
  - 若为租赁且首期已收 → 自动核销 `repayments` 第 1 期(`status='已还款'`,流水号写为 `INITIAL-{payment_id}`,避免重复入账)。
    > **第 1 期逾期边界(v3.1-v2 补充)**:租赁第 1 期 `due_date = 起租日`。若 G2 审批日晚于起租日(客户先打款、财务隔几天才对账),为避免 H1 在核销前把第 1 期误判 `逾期N日` 并计提万5/日——**出库前(`delivery_status != 已出库`)的第 1 期不纳入 H1 逾期计提**;G2 核销后该期即为 `已还款`,自然退出扫描。
  - **目的**:G3 出库校验 contracts 的 `down_payment_status='已收'` 与 `deposit_status='已收'` 据此通过;若不写回,G3 永远卡死。
  - **代码定位**:`app.py:3262~3318` 已实现该逻辑,文档此处仅作显式说明。

### G3 车辆出库(决策项 35)
- **触发**:G2 财务审批通过后,车管端"出库"。
- **输入**:**出库照片(必填,至少 3 张:车头/车尾/驾驶位仪表)**、出库单 PDF(选填)。
- **规则**:
  1. **出库门禁按 `contract_type` 区分(⑥)**:
     - **以租代售(`销售`)**:校验 `contracts.down_payment_status='已收'`(首付款=购车款已到账)。首付款是计划表独立行,不并入第一期月供;**首付款到账即可出库**,不要求第一期月供已还。
     - **租赁(`租赁`)**:校验 `contracts.deposit_status='已收'` **且** 第一期租金已收(`repayments` 第 1 期 `status='已还款'`,由 G2 自动核销写回)。**押金 + 第一期租金都到账才可出库**。
  2. 写 `delivery_date / delivery_photo_path`(JSON 数组),`delivery_status='已出库'`。
  3. 车辆状态:租赁=>`租赁中`;以租代售=>`以租代售`。
- **异常**:首款未到账时拒绝出库;租赁缺第一期租金核销时拒绝出库;出库照片少于 3 张拒绝提交。
- **决策项 35**:出库照片是后续退车验车残值判定的基线,**前端硬校验数量并提示拍摄部位**。

---

## 模块 H — 每日监控与催收

### H1 日终批处理(系统每日 00:30,v3.1-v2 重写为逐日"逾期N日"状态机)

> **业务口径(本次确认,统一算法)**:每天 00:30 把**所有订单**按**最初的应还日**重算一遍 `diff`,状态严格逐日推进——T-3/T-2/T-1 都预警(`临近还款`)、T 日为 `还款日`、T+1 起为 `逾期1日 / 逾期2日 / …` 一直累加。**不还或未还足额,欠款一直按万5/日累计,首付款与押金一律不可抵扣。** 取消旧版的 `待催款` 中间态与"资金池可冲/耗尽/无可抵冲"三态。

- **触发(v3.1-v2 修订)**:统一入口 `POST /api/jobs/daily-collect`,由两条路径调用:
  1. **定时(主):** 进程内调度器(APScheduler)每日 **00:30** 自动调用一次;单机部署用进程内定时器,多实例部署改挂系统级 cron/计划任务调同一端点。
  2. **惰性兜底:** 关键查询入口(看板统计等)进入时,若检测到"当天尚未跑过日终批",补跑一次。
  - **防重 + 补跑**:以 `(job='daily-collect', run_date)` 为幂等键,**当天只实际执行一次**;若某天因停机漏跑,下次触发时按 `diff` 区间式状态机自动把单子推进到正确档位(见下方③),不会因漏跑卡死。
  > **修订原因**:原文档声明"Cron 00:30 调 `daily-collect`",但代码中该端点不存在,`check_overdue` 实际仅在看板查询(`app.py:1046` 等)被动触发——无人访问当天就不计提、不推催。本版把定时入口显式化,并保留惰性兜底 + 当天幂等。
- **扫描范围(v3.1-v2 修订)**:
  ```
  status ∈ {
    待还款, 临近还款, 还款日, 部分核销, 预抵,
    逾期1日, 逾期2日, …, 逾期N日   ← 逐日推进,不分业务模式,欠款持续累计
  }
  ```
  > **修订原因**:取消旧版按 `contract_type` 拆分的"资金池可冲/耗尽/无可抵冲"三态与"待催款"中间态。新口径下租赁与以租代售在逾期处理上**一致**——都只逐日记 `逾期N日` + 计提万5/日,差别仅在退车/结清环节,不在催收周期。
  > **注**:`未激活`(F1 落库默认值,见 F1/F4)**不在扫描范围**,合同 F4 激活后才改 `待还款` 进入监控,避免对未生效计划误计滞纳金。**`已还款`/`预抵` 纳入扫描仅做状态复核(确认已结清、不回退),不参与 `diff` 逾期推进、不计滞纳金。**
- **处理顺序(按"今天 − 最初应还日"逐日推进,v3.1-v2 重写)**:
  ```
  diff = today − due_date          # 负数=未到期,0=还款日当天,正数=已逾期天数

  # ① 临近预警(还款日前 3 天内,逐日预警)
  -3 ≤ diff ≤ -1 且 status ∈ {待还款}
    → status = '临近还款'          # T-3/T-2/T-1 都进预警名单(亮预警灯)

  # ② 还款日当天
  diff = 0 且 status ∈ {待还款, 临近还款, 部分核销}
    → status = '还款日'

  # ③ 逾期逐日推进(不分业务模式)
  diff ≥ 1 且 未结清
    → status = '逾期' + diff + '日'        # 逾期1日 / 逾期2日 / … / 逾期N日
    → 写/更新 late_fee_ledger 当日累计行:
        基数 = 当期原始未还本金(amount − paid_amount),首付款/押金一律不抵扣
        当日 = 基数 × 0.0005(万分之5/日),逐日累加,无复利
    → 同步 contract_fee_items(fee_type='late_fee').amount_due = cumulative

  # ④ 催款节点(只推送任务,不改抵冲、不动押金/首付款)
  diff = 3  → 推送【运营催款】任务 + 文案(运营线下催,上传截图)
  diff = 7  → 推送【销售催款】任务 + 文案(销售线下催,上传截图)
  diff > 7 且仍未还清 → 保持 '逾期N日',欠款 + 滞纳金继续逐日累计,直至客户实际还清
  ```
  > **③ 说明(本版重写)**:旧版用抽象档位(`待催款`/`资金池可冲/耗尽/无可抵冲`)。新版按业务方口径改为**逐日 `逾期N日`**——每天 00:30 全量重算 `diff`,状态名直接反映逾期天数;漏跑补跑时按当日 `diff` 一次推到正确档位,不会卡死。
  > **④ 说明**:T+3 运营催、T+7 销售催**只是推送催款任务**(H2),不再触发任何"押金/首付款抵冲"。逾期欠款与滞纳金一路累计,**只能靠客户真实还款清偿**(押金仅在退车结算 M、首付款不可挪用)。
  > **① 说明**:`临近还款` 为 T-3~T-1 的统一预警态,是 H1 唯一预警来源(见下方 T-3 提醒名单,⑪)。
- **T-3 提醒文案模板**(占位符由系统填充,v2 版本保留):
  ```
  尊敬的{客户姓名},您的车架号为{VIN}的车辆将于{应还日期}进行还款,
  本期应还金额 ¥{应还金额}(第{期数}/{总期数}期),
  请您按时将款项转入收款账户:{收款公司}-{银行}-{账号}。
  如已支付请忽略本提醒,感谢您的配合。
  ——{公司名称}
  ```
  - **运营线上通知动作**:工作台"今日 T-3 预警"列表**统一以 `repayments.status='临近还款'` 为唯一数据源**(不再各处用 `diff` 现算,避免"状态已临近但列表漏列");运营点【发送提醒】可选短信/微信/复制文案;文案允许编辑,发送后写 `notification_logs`。同期可多次发送,工作台只显示"未提醒"列表(`status='临近还款'` 且无当期 `notification_logs`);已提醒用绿色徽标标记。
    > **⑪ 说明(本版统一)**:H1 状态机标记与工作台预警列表必须同源——谁被状态机标成 `临近还款`,列表就显示谁;判定口径只有 H1 状态机一处,前端不再重复用日期计算,杜绝两套算法对不上导致漏提醒。
- **输出**:任务推送到对应工作台 + audit_logs。
- **代码改造提示**:`app.py:1018~1033` 当前 `check_overdue` 仅做"过期 → 逾期"一步,需重写为上面的**逐日 `逾期N日` 状态机**(每日全量重算 `diff`,按区间映射到 `临近还款 / 还款日 / 逾期N日`)。迁移时把存量 `逾期` 一律按 `diff` 回填为对应 `逾期N日`,不再区分业务模式、不再生成"资金池可冲/耗尽/无可抵冲"等旧值。
  - **⑨ 补充(本版)**:`check_overdue` 不止函数定义处,实测有 **3 个调用点**——`app.py:1046`(看板统计 `get_stats`)、`app.py:2085`、`app.py:2158`。重写函数行为后这 3 处语义都会变,需逐一核对;并结合上方"触发方式"决策——把状态机逻辑收口到 `POST /api/jobs/daily-collect` 单一入口,3 处调用改为"当天未跑则补跑"的惰性兜底,不再各自独立执行更新。

### H2 催款执行
- **运营(T+3)**:打电话/上门后上传催款单截图 → `urge_records(urge_type='运营催款', urge_day=3)`。
- **销售(T+7)**:同上 → `urge_records(urge_type='销售催款', urge_day=7)`。
- **规则**:必须上传截图才能"标记已催"(运营/销售对客户面,截图为客户接触凭证,决策项 3)。
- **SLA**(决策项 20):
  - T+3 当日运营未催 → 次日通知运营主管。
  - T+7 当日销售未催 → 次日通知销售主管 + 老板看板红色徽标。

### H3 逾期收款与客户还款清偿瀑布(v3.1-v2 重写:首付款/押金均不可抵扣)

> **v3.1-v2 关键修订(本次确认)**:**首付款与押金一律不能作为抵扣资金。** 取消旧版"押金 + 客户预收"组成抵冲池、逾期自动抵冲的整套模型。无论租赁还是以租代售,逾期处理完全一致:
> - **不做任何自动抵冲**——不动押金、不动首付款、不动客户预收;
> - 逾期欠款 + 万5/日滞纳金**逐日累计**(见 H1 `逾期N日`、J1);
> - **只能靠客户真实还款清偿**(`repayments.paid_amount` 仅随实际到账上升);
> - 押金只在**退车结算**(M 模块)时抵扣违章/损耗/未还款,首付款是购车款不可挪用;
> - 未结清则**禁止过户/结清**(N 模块拦截)。

- **H3 触发后系统动作(逐日,直至客户还清)**:
  1. 继续按 J1 计提 `late_fee_ledger` 万5/日滞纳金(基数 = 当期原始未还本金,无复利);
  2. 还款单保持 H1 的 `逾期N日`,推送销售/财务"催款"任务(T+7 销售催,见 H2);
  3. **禁止过户/结清**:只要存在任一期未结清,N 模块过户/结清校验(N1/N3)一律拦截;
  4. 客户实际到账后走 I2 核销,`paid_amount` 上升,期次结清后退出 `逾期N日`。
- **客户主动还款时的清偿顺序(I2 核销中执行,非系统自动扣池)**:当客户**实际打款**且金额有富余,按以下优先级清偿现存未付应收(每笔写 `reconciliation_allocations`):
  1. 保险/代办类(insurance_fee, service_fee)
  2. 维修/事故/罚金类(maintenance_fee, accident_fee, penalty_fee)
  3. 本期租金(rent)
  4. 滞纳金(late_fee,优先级最低)
  > **注**:此顺序只在"客户真金白银还款且有富余"时对**到账资金**生效;系统**不会**用押金/首付款去顶。这与旧版"资金池自动抵冲"有本质区别。
- **滞纳金减免守卫(决策项 12,保留)**:客户还款抵冲滞纳金前,查 `waivers(target_period=该期, waiver_kind='late_fee', status IN ('待审批','已通过'))`:
  - 存在 → **本期滞纳金本次不抵冲**,标记"待减免审批,本期暂缓",写 audit_logs;
  - 守卫超时(默认 48h 未生效)→ 解除,按未减免处理;老板事后批准走"反向减免登记"冲正;
  - **目的**:避免"客户还款抵了滞纳金 + 第二天又批准减免"造成同一笔重复冲销。
- **输出**:还款明细、剩余欠款、滞纳金累计/已减免;**不再有"已抵冲(押金)"口径**。
- **界面要点**:运营/财务能看到"客户还款清偿瀑布"(含"减免审批中暂缓"分支);老板视图可穿透到任意一笔。**界面不再展示"押金抵冲池余额"。**

### H4 锁车 / 解锁(v3.1-v2 重写:标记式审批流,系统不远程控车)

> **业务口径(本次确认)**:系统里的"锁车/解锁"**只是一个状态标记动作**,真正锁/解锁车辆由**销售在第三方控车系统**完成。平台**永不远程控车**,只记录"谁申请、谁审批、当前到哪一步、最终结果"。

- **锁车状态字段(新增)**:车辆锁定状态与 `vehicles.status`(在库/租赁中/…)**相互独立**,单列 `vehicles.lock_status`:
  ```
  未锁 / 锁车流程中 / 车辆已锁 / 开锁流程中
  ```
  一辆 `租赁中` 或 `以租代售` 的车,可同时处于 `车辆已锁`;锁定与否不改变 `vehicles.status` 主状态。

#### H4-1 锁车流程
- **触发**:H3 逾期推送(`diff≥7` 仍未还清,或销售判定需施压)→ 销售工作台显示"建议锁车"红色徽标 + 原因(欠款金额、逾期天数、累计滞纳金);销售点【申请锁车】发起。
- **前置校验**:对应期数已存在 H2 催款记录。
- **审批链**:销售申请 → **运营通过** → **老板审批**(`lock_requests.ref_type='lock'`)。
- **状态流转**:
  1. 销售发起 → `lock_requests(action='lock', status='待运营审核')`,车辆 `lock_status='锁车流程中'`。
  2. 运营通过 → `status='待老板审批'`(`lock_status` 仍为 `锁车流程中`)。
  3. 老板通过 → `status='已批准待执行'`(`lock_status` 仍为 `锁车流程中`);若任一环节驳回 → `status='已驳回'`,`lock_status` 回退 `未锁`。
  4. **销售到第三方控车系统实际锁车** → 锁车成功后回到平台点【锁车】确认按钮 → `lock_status='车辆已锁'`,`lock_requests.status='已完成'`,记录 `locked_by/locked_at`。
- **输出**:`lock_requests` 全程留痕 + audit_logs;车辆列表/详情展示 `lock_status` 徽标。

#### H4-2 解锁流程(对称)
- **触发**:客户结清/还款到位后,销售点【申请解锁】发起。
- **审批链**:销售申请 → **运营通过** → **老板审批**(`lock_requests.ref_type='unlock'`)。
- **状态流转**:
  1. 销售发起 → `lock_requests(action='unlock', status='待运营审核')`,车辆 `lock_status='开锁流程中'`。
  2. 运营通过 → `status='待老板审批'`;老板通过 → `status='已批准待执行'`(`lock_status` 仍为 `开锁流程中`);任一环节驳回 → 回退 `车辆已锁`。
  3. **销售到第三方控车系统实际解锁** → 成功后回平台点【解锁】确认按钮 → `lock_status='未锁'`,`lock_requests.status='已完成'`,记录 `unlocked_by/unlocked_at`。
- **输出**:同上,全程留痕。

> **说明**:`lock_status='锁车流程中' / '开锁流程中'` 是"审批+待销售线下操作"的过渡态;只有销售点确认按钮后才落到终态(`车辆已锁` / `未锁`)。平台不调用任何远程控车接口。

---

## 模块 I — 还款确认与计划表调整

### I1 运营登记本期还款(v2 修订,决策项 7)
- **触发**:客户支付后,运营点【本期还款】。
- **输入**:
  - **必填**:期数、实收金额、**客户付款截图**(运营对客户面,截图必填)。
  - **多还场景必填**:抵扣月份选择(运营与客户当面/电话确认,前端弹框记录确认时间)。
- **规则**:
  - 写 `repayments.paid_amount`、`screenshot_path`,状态置 `部分核销`(待财务核对)。
  - **决策项 7**:**多还抵扣月份的客户确认在 I1 阶段完成,运营负责留痕**;前端弹框需勾选"已与客户确认抵扣月份",写 `repayments.extra_alloc_confirmed_by/at` 与 `repayments.extra_alloc_periods`。
  - 缺少客户截图 → 前端禁用提交。

### I2 财务核销(决策项 7、15)
- **触发**:I1 上传后,财务点【核销确认】。
- **输入**:
  - **必填**:银行流水号、实际到账金额。
  - **选填**:公司收款截图。
- **规则**(三分支):
  1. **等额**(`received == amount`):`status='已还款'`,计划表不变。
  2. **少还**(`received < amount`):
     - 当期 `paid_amount=received`,`status='部分核销'`。
     - 差额 `gap = amount - received` 累加到下一期 `repayments[period+1].amount += gap`。
     - 若已是末期则进入退车结算 / 提前结清(押金仅在退车 M 模块抵扣,首付款不可挪用)。
     - **v3.1-v2 修订**:删除旧版"差额累计上限 = 押金/首付款抵冲能力"的口径——首付款/押金均不可抵扣,差额只是顺延为客户欠款,持续累计直至客户真实还清(逾期期间按 J1 计提万5/日)。
  3. **多还**(`received > amount`):
     - 当期 `status='已还款'`。
     - 多余 `extra = received - amount`。
     - **先冲应收再预抵(铁规)**:这是**客户真实多打的款**,在执行预抵前按清偿优先级抵冲现存未付应收:
       ```
       extra 依次抵冲(直到耗尽或无应收为止):
         ① 保险/代办类 应收差(insurance_fee, service_fee)
         ② 维修/事故/罚金类 应收差(maintenance_fee, accident_fee, penalty_fee)
         ③ 已计提未冲滞纳金(late_fee_ledger 中 outstanding 部分,受 J3 守卫:存在审批中减免则跳过)
       每笔写 reconciliation_allocations(allocation_type 对应 fee_type)。
       ```
     - 应收清偿后**仍有余额**,才**按 I1 已确认的抵扣月份**,系统把对应后续 N 期 `status='预抵'`、`paid_amount=amount`。
     - 仍抵不完的余额记为 `客户预收`(`customer_prepayments` 表),仅供该客户**后续期次还款时抵充**。
     - **v3.1-v2 强调**:此处抵冲的来源是**客户真金白银多打的款**,与"押金/首付款不可抵扣"不矛盾——系统永远不会动押金或首付款去顶欠款。`客户预收` 只是客户自己多付的钱挂账,不属于押金池。
     - **决策项 7**:I2 阶段不再让财务"二次确认抵扣月份",避免责任错位。
- **输出**:`bank_serial`、`verified_amount`、`waterfall_summary` 全量写入。
- **决策项 15**:**财务责任口径 = 流水真实性、金额一致性**,**不再承担"是否抵扣哪几期"决策**(那是运营+客户共同确认的)。
- **审计**:每次核销均写 `reconciliation_allocations`。

### I3 计划表展示
- **界面要点**:
  - 展示客户表 + 厂家表两栏对照;
  - 已"预抵"期数加灰色徽标 + 抵扣来源期数提示;
  - 当期"差额顺延"用箭头连接后期。

---

## 模块 J — 滞纳金

### J1 计提规则(逾期即收,决策项 5)
- **触发**:H1 日终批处理(`due_date < today` 且当期未结清),即 `diff = today - due_date ≥ 1`。
- **起算口径(本版明确,无宽限期)**:**滞纳金自逾期第 1 天(`diff = 1`,应还日次日)起计提**,不设任何宽限期;与 H1 状态机 `diff ≥ 1` 计提分支完全一致。注意此处与 H2"T+3 运营催款 / T+7 销售催款"是**两套独立节奏**——催款任务晚启动,但滞纳金从逾期第一天就开始累计。**(v3.1-v2:旧版"H3 T+7 才抵冲"逻辑已删除,系统任何节点都不动押金/首付款。)**
- **公式**:
  ```
  outstanding(t) = amount - paid_amount(t)        # 原始未还本金,不扣押金抵冲
  daily_amount   = outstanding(t) × 0.0005        # 万分之5/日
  cumulative     = Σ daily_amount                 # 从逾期第1天(diff=1)起逐日累加,无复利
  ```
- **决策项 5(关键,v3.1-v2 已按 PDF 口径重写)**:
  - 滞纳金计提基础**永远是当期原始 `amount - paid_amount`**(未还本金),**首付款与押金一律不参与计提基数的任何抵冲**。
  - **取消旧版"H3 押金/首付款顺序扣款"**:系统在任何节点都不会用押金或首付款去顶欠款或减少计提基数。唯一能减少 `outstanding` 的,是客户真实到账还款。
  - **基数随客户真实还款递减**:客户每还一笔,`paid_amount` 增大,次日计提基数 = `amount - paid_amount` 相应变小;未还清则继续逐日计提。
  - **不计复利**:滞纳金本身的逾期不再产生新的滞纳金。
- **写入**:`late_fee_ledger(repayment_id, accrued_date, outstanding, daily_amount, cumulative_amount)`,`UNIQUE(repayment_id, accrued_date)` 防重。
- **同步落账**:每日批处理同时写/更新 `contract_fee_items(fee_type='late_fee')`,`amount_due = cumulative` 并同步状态,使其进入正式应收口径。

### J2 抵冲与收取
- **逾期 = 应收**:滞纳金一旦计提即为客户应付债务,需要收取。
- **收取顺序(仅对客户真实到账资金生效)**:客户主动还款时,若本期款项有富余且没有更高优先级未结项,按"先冲应收本金/租金、再冲滞纳金"的顺序抵销已计提滞纳金;`late_fee` 排在租金之后。**系统不会用押金/首付款去抵滞纳金或欠款**——这与旧版"资金池自动抵冲"有本质区别。
- **结清前提**:合同结清(自然/提前/退车)前,若 `late_fee_ledger` 存在未抵冲累计金额,必须走 J3 减免审批,否则视为应收,并入退车押金扣除或提前结清买断金额。

### J3 减免审批(销售发起 → 老板审批 → 财务执行)
- **触发**:销售端"滞纳金减免申请"(可在还款过程中、退车节点、提前结清节点发起)。
- **输入**:合同 ID、命中期数(可多选/全选)、减免金额或全额减免、原因、附件(可选)。
- **数据表**(决策项 30,统一表名 `waivers`;v3 改 RENAME 为 CREATE):
  ```sql
  -- v2 原方案是 ALTER TABLE rent_waivers RENAME TO waivers,但 database.py
  -- 中并不存在 rent_waivers 表,执行会报 "no such table" 直接失败。
  -- v3 改为直接 CREATE,迁移脚本无前置依赖。
  CREATE TABLE IF NOT EXISTS waivers (
      id                   INTEGER PRIMARY KEY AUTOINCREMENT,
      contract_id          INTEGER NOT NULL,
      waiver_kind          TEXT NOT NULL DEFAULT 'rent',   -- 'rent' | 'late_fee'
      target_period_list   TEXT,                            -- JSON: [1,2,3] 或 'all'
      waive_amount         REAL,                            -- 减免金额(全额减免时填 NULL)
      reason               TEXT,
      attachment_path      TEXT,
      status               TEXT DEFAULT '待审批',           -- 待审批/已通过/已生效/已驳回/已撤销
      sales_applied_by     TEXT, sales_applied_at     TEXT,
      boss_approved_by     TEXT, boss_approved_at     TEXT,
      finance_reviewed_by  TEXT, finance_reviewed_at  TEXT,
      revoke_reason        TEXT,
      FOREIGN KEY(contract_id) REFERENCES contracts(id)
  );
  CREATE INDEX IF NOT EXISTS idx_waivers_contract  ON waivers(contract_id);
  CREATE INDEX IF NOT EXISTS idx_waivers_status    ON waivers(status);
  -- ⑪ 复合索引:H3 抵冲守卫(按合同+kind+status 查"审核中"减免)与 N1 结清守卫高频命中
  CREATE INDEX IF NOT EXISTS idx_waivers_contract_kind_status
      ON waivers(contract_id, waiver_kind, status);
  ```
  > **⑪ 说明**:H3 抵冲前的"减免审批中"守卫(本文档 H3)与 N1 结清前的"关闭审核中减免"守卫(⑧)都按 `(contract_id, waiver_kind, status)` 过滤 `status IN ('待审批','已通过')`。单列索引 `idx_waivers_contract`/`idx_waivers_status` 不能覆盖该组合谓词,日终批处理逐合同扫描时会退化为表扫;补此复合索引保证守卫查询走索引。
- **流程**:
  1. **销售发起** → `waivers(waiver_kind='late_fee', status='待审批')`,推送老板待办。
  2. **老板审批** → 通过/驳回 + 备注;通过后 `status='已通过'`,推送财务待办。
  3. **财务执行**:
     - 在 `late_fee_ledger` 中按命中期数标 `waived=1`、`waived_amount=...`(新增字段)。
     - 同步 `contract_fee_items(fee_type='late_fee').amount_due -= waive_amount`。
     - 写 `reconciliation_allocations(allocation_type='late_fee_waiver', allocated_amount=waive_amount)`。
     - `waivers.status='已生效'`,`finance_reviewed_by/at` 落库。
- **决策项 12(v3.1-v2 修订)**:减免只核减已计提滞纳金本身——已减免部分从 `late_fee_ledger`/`contract_fee_items` 中扣除,不再计入应收;未减免部分继续按 J1 逐日计提,**仅由客户真实到账还款按 I2/J2 清偿瀑布冲减**。**(旧版"按 H3 优先级 4 抵冲"的押金/首付款抵冲逻辑已删除,此处不再引用 H3。)**
- **撤销规则**:仅"已通过未生效"可撤销;"已生效"需通过"反向减免登记"补登。
- **通知**:推送给销售、运营、客户对账页。

### J4 结算时机(v2 修订,决策项 2)
- **退车节点(M 模块)**:
  - 若存在未减免/未抵冲的 `late_fee` 累计金额,M3 运营查数据时自动汇总到 `total_deduction`。
  - 销售如需减免,**必须在 M1 之前或 M3 之前完成 J3 流程**,得到老板批复后方可在退车单中体现。
  - **决策项 2**:**不再由销售在退车单上单点决定**,`return_inspections.waive_late_fee` 字段仅用于展示是否已经走完 J3(只读标记位)。
- **以租代售结清/提前结清(N 模块)**:同上,需提前完成 J3 才能进入过户。

### J5 展示
- 合同详情页"滞纳金累计"卡片:本月新增 / 累计金额 / 起算日 / 已抵冲 / 已减免 / 待收。
- 老板穿透:可查任一车辆的滞纳金 ledger 与减免审批链路。

---


## 模块 K — 租金减免(决策项 30)

> 与 J3 共用 `waivers` 表,`waiver_kind='rent'` 区分。

### K1 销售发起申请
- **触发**:销售端"租金减免申请"。
- **输入**:合同、目标期数、减免金额、原因、附件(可选)。
- **规则**:
  - 减免金额不得超过该期 `amount - paid_amount`。
  - 写 `waivers(waiver_kind='rent', status='待审批')`。

### K2 老板审批
- **触发**:K1 提交后老板待办。
- **规则**:通过/驳回 + 备注;写 `boss_approved_by/at`,`status='已通过'/'已驳回'`。
- **SLA**(决策项 20):48h 未审批 → 销售可发催办,催办 3 次仍无果 → 推送老板看板红色。

### K3 财务复核 + 落库
- **触发**:K2 通过后财务待办。
- **规则**:
  1. 复核后 `status='已生效'`,`finance_reviewed_by/at`。
  2. 系统更新 `repayments[target_period].amount -= waive_amount`(下一期付款时直接体现)。
  3. 写 `reconciliation_allocations(allocation_type='waiver', allocated_amount=waive_amount)`。

### K4 通知
- 推送给销售、运营,客户对账时可看到减免说明。

### K5 撤销
- 仅"已通过未生效"可撤销;"已生效"不可撤销,需走"反向减免"补登。

---

## 模块 L — 发票申请

### L1 运营发起(决策项 28)
- **触发**:运营端"申请发票"按钮(仅运营可见,合同首期/任一已收款期均可)。
- **输入**:
  - **开票主体(必填,⑦)**:申请人填写**开票主体名称** + **统一社会信用代码**。
  - 收款公司名称(联动模块 R)、抬头类型(企业/个人)、金额、对应期数。
  > **⑦ 关键修订——开票主体与合同主体解耦**:合同签约主体与实际开票主体**经常不一致**(如合同挂 A 公司,客户要求开 B 公司抬头)。**系统不强制开票主体 = 合同主体**,以申请时填写的开票主体 + 统一社会信用代码为准。`invoice_requests` 新增 `invoice_entity_name`、`invoice_entity_tax_no` 两列承载申请填写值。
- **前置审批**(决策项 28):
  - **首期发票**:必须该期 `repayments.status='已还款'` 才能申请。
  - **后续期数**:同上,只允许"已确认到账"的期数申请发票,防止开票后客户违约。
  - **大额发票**(配置阈值,默认 ≥ 5 万):需老板审批节点,审批通过后才进入财务待开票队列。
- **规则**:写 `invoice_requests(status='待开票', invoice_entity_name, invoice_entity_tax_no)`(若需老板审批则 `status='待老板审批'`)。

### L2 财务开票
- **触发**:财务待办"待开票"。
- **输入**:**发票号(必填,长度 ≥ 8,字母数字)**、开票时间(必填)、发票 PDF/截图(选填,决策项 3)。
- **规则**:财务**按 L1 申请填写的 `invoice_entity_name` / `invoice_entity_tax_no` 开票**(⑦:不回查合同主体、不强制一致),`status='已开票'`,写 `invoiced_at`、`processed_by`。
  > **⑦**:若财务核对发现开票主体明显有误,退回运营改 L1 申请,**不在 L2 私自改主体**,保证开票主体始终由申请环节决定、可追溯。

### L3 发票纠错(作废 vs 红冲,决策项 29)
- **场景区分**:
  - **作废(本月内)**:发票尚在当月、且未跨账期 → 走"作废"动作,等价于"未开过"。
  - **红冲(跨月/已申报)**:发票已跨月或已纳入纳税申报 → 必须走"红冲发票"流程,不可直接作废。
- **触发**:财务在已开票记录上点"作废"或"红冲"按钮(系统按开票时间自动判断默认动作,允许财务覆盖)。
- **输入**:
  - 作废:作废原因(必填)、作废凭证(选填)。
  - 红冲:红冲原因(必填)、红冲蓝字凭证(必填,作为税务凭据)、红冲日期。
- **流程**:
  1. **作废**(适用本月内):`invoice_requests.status='已作废'`,记录 `voided_by/voided_at/void_reason`。系统保留原发票号、开票时间用于审计追溯,**不可删除**。
  2. **红冲**(适用跨月):`invoice_requests.status='已红冲'`,记录 `red_invoice_no`(红冲蓝字票号)、`red_invoiced_at`、`red_reason`、`red_certificate_path`(必传)。
  3. **重新上传(可选)**:作废/红冲后系统自动派生一条同合同同期数的新记录(`status='待重开'`,`previous_invoice_id` 指向被作废/红冲的记录),进入财务待办。
  4. **重开提交**:财务在重开记录上重新填写发票号(必填)、开票时间(必填)、发票 PDF/截图(选填),提交后 `status='已开票'`。
- **数据模型扩展**:
  ```sql
  ALTER TABLE invoice_requests ADD COLUMN voided_by TEXT;
  ALTER TABLE invoice_requests ADD COLUMN voided_at TEXT;
  ALTER TABLE invoice_requests ADD COLUMN void_reason TEXT;
  ALTER TABLE invoice_requests ADD COLUMN previous_invoice_id INTEGER;
  -- 红冲专用字段
  ALTER TABLE invoice_requests ADD COLUMN red_invoice_no TEXT;
  ALTER TABLE invoice_requests ADD COLUMN red_invoiced_at TEXT;
  ALTER TABLE invoice_requests ADD COLUMN red_reason TEXT;
  ALTER TABLE invoice_requests ADD COLUMN red_certificate_path TEXT;
  -- 开票主体字段(⑦:与合同主体解耦,L1 申请时填写)
  ALTER TABLE invoice_requests ADD COLUMN invoice_entity_name TEXT;     -- 开票主体名称
  ALTER TABLE invoice_requests ADD COLUMN invoice_entity_tax_no TEXT;   -- 统一社会信用代码
  ```
- **可作废/红冲的状态**:仅 `已开票` 可作废/红冲;`待开票`、`已作废`、`已红冲` 不可再次操作。
- **审计**:作废、红冲、重开均写 `audit_logs`,操作人、时间、原因全量记录;客户对账页能看到完整版本链(蓝色发票 / 灰色作废 / 橙色红冲徽标)。

### L4 客户对账
- 合同详情"发票记录"卡片展示有效发票(`status='已开票'` 且未被作废/红冲)+ 历史版本链;运营可下载有效版本。

---

## 模块 L-附 — 对账与上传规则(全局,决策项 3、25、31)

> **核心原则(决策项 3)**:
> - **运营 / 销售面对客户的环节,客户截图必填**(凭证留痕,客户面交付证据)。
> - **财务面对公司账户的环节,流水号 / 发票号必填**(金额追溯唯一锚点),公司截图选填。

### 必填字段(所有对账场景统一)

| 场景 | 必填 | 选填 |
|---|---|---|
| 财务确认还款核销(I2) | 银行流水号、实际到账金额 | 公司收款截图 |
| 财务上传公司首款(G2 财务侧) | 银行流水号、到账金额 | 公司收款截图 |
| 运营申请发票(L1) | 开票主体名称、统一社会信用代码、金额、对应期数 | — |
| 财务开票(L2) | 发票号、开票时间 | 发票 PDF/截图 |
| 财务红冲(L3) | 红冲蓝字票号、红冲日期、红冲蓝字凭证 | — |
| 提前结清/买断收款(N2 财务侧) | 银行流水号、收款金额 | 收款截图 |
| 退车押金退款/扣减(M6) | 银行流水号(若有出款)、扣减金额明细 | 出款回单截图 |
| 运营登记客户付款(I1、G2 运营侧) | 客户付款金额、期数、**客户付款截图** | — |
| 运营催款记录(H2 T+3) | 催款时间、沟通方式、结果备注、**催款截图** | — |
| 销售催款记录(H2 T+7) | 催款时间、沟通方式、结果备注、**催款截图** | — |
| 销售发起减免申请(J3、K1) | 期数、金额、原因 | 附件 |

### 通用规则(决策项 25、31;v3 修订:bank_serial 改幂等键、发票号允许 NULL)
- **流水号幂等键(v3 修订决策项 31)**:同一银行流水号允许在不同期/不同合同上重复出现(支持"一笔款分摊多笔登记"),但**同一(`bank_serial`,`contract_id`,`period`)三元组只允许一条登记**——
  - "首款流水"维度:`contract_initial_payments` 复合幂等键 = `(bank_serial, contract_id)`,UNIQUE。
  - "还款核销"维度:`repayments` 复合幂等键 = `(bank_serial, contract_id, period)`,UNIQUE。
  - "提前结清"维度:`contracts.early_settlement_serial` 仍为合同维度唯一(单合同结清一次)。
  - **修订原因**:v2 把 `repayments.bank_serial` 单列设为 UNIQUE,与 L-附自身"同一笔款分摊多笔登记"的场景直接打架。v3 改为复合键,既防重复入账,又允许跨期分摊。
- **流水号长度(决策项 25)**:不少于 6 位,允许字母数字混合,前后空格自动 trim;系统对常见银行格式给出宽松正则校验。
- **发票号格式(决策项 25;v3 修订:允许 NULL)**:不少于 8 位,允许字母数字。
  - `invoice_requests.invoice_no` **允许 NULL**(L1 申请阶段尚无发票号,L2 财务开票时才填写)。
  - **唯一约束**:`UNIQUE(invoice_no) WHERE invoice_no IS NOT NULL`(SQLite 部分索引,或在应用层校验:开票成功时强制非空 + 全局唯一)。
  - **修订原因**:v2 写"全局唯一"未明确允许 NULL,L1 插入会失败。
- **截图为选填(财务侧)**:不阻塞流程,但若上传则必须是图片或 PDF;系统留存附件路径供后续审计。
- **截图为必填(运营/销售侧)**:前端硬校验文件存在,后端兜底。
- **审计**:必填字段缺失时前端禁用提交按钮,后端兜底校验返回 400。

---

## 模块 M — 退车(到期 / 提前)

### M1 销售发起退车
- **退车流程总览(本次确认)**:**销售发起退车 → 车到公司后车管验车填单(M2)→ 运营查车辆相关数据填单(M3)→ 财务复核(M4)→ 领导审批(M5)→ 出款+入库(M6)**。
- **触发**:销售端"发起退车"。
- **租赁末期触发锚点(本版新增)**:租赁合同(`contract_type='租赁'`)最后一期(`period == loan_periods`)核销完成后,**系统自动在销售工作台生成"待退车"提示**(列出合同号、车辆、末期核销时间),作为销售发起 M1 的锚点;合同保持 `执行中`、车辆保持 `租赁中`,**不进入 N1、不生成过户单**(见 N1 守卫)。提示仅为待办,实际退车仍由销售点【发起退车】启动本流程,避免合同/车辆状态停在末期永不收口。
- **场景**:① 租期到期且末期已收;② 客户申请提前退车。
- **输入**:
  - 退车原因(到期退车/提前退车/租赁转购车/临时用车)、附件。
  - **决策项 16**:**提前退车违约金**输入字段(可选,有约定时由销售填写,展示给后续 M3 运营、M4 财务复核)。
  - **决策项 2**:**不再由销售在此勾选"是否减免滞纳金"**;若有减免需求,需提前完成 J3 减免审批,本节点仅展示"滞纳金累计 / 已减免"两个只读字段。
- **规则**:写 `return_inspections(sales_status='已登记')`,合同状态=`退车中`,车辆状态=`退车中`(决策项 24,新增显式状态)。

### M2 车管验车
- **触发**:车回库后车管端"验车"。
- **输入**:
  - 随车工具(三角架/反光衣/灭火器/楔块/千斤顶)、证件钥匙。
  - 公里数、车身/轮胎清洁、事故/违章/ETC/维修保养、出险加保费。
  - **附件**:验车现场照片(必填,与 G3 出库照片对比基线,决策项 35)。
  - 若需维修勾选"待维修"。
- **规则**:`fleet_status='已填写'`,无问题 → 待运营查数据;有维修 → **车辆状态置 `待维修`**(决策项 6,只有此处能进入 `待维修`)。

### M3 运营查数据并填单
- **触发**:M2 完成后。
- **输入**:违章/ETC/事故等明细、应扣金额、押金抵扣项;**滞纳金应收(只读,从 J1 ledger 自动汇总)**。
- **规则**:`operator_status='已填写'`,自动汇总 `total_deduction`、`actual_refund`。

### M4 财务复核押金(决策项 16)
- **触发**:M3 完成后。
- **规则**:
  - **到期退车**:`actual_refund = deposit_paid - total_deduction(含未减免滞纳金)`,`status='待退款'`。
  - **提前退车**:
    - 若 M1 填写了违约金 → 加入 `total_deduction`。
    - 若押金不够覆盖扣减 → 差额记 `accounts_receivable`,推送销售去追收。
  - `finance_status='已填写'` + `finance_approved=1`。

### M5 领导审批
- **触发**:M4 完成后,**领导审批 = 老板**(`approval_flows.ref_type='return_stock'`)。
- **规则**:通过=`boss_approved=1`,允许出款。

### M6 出款 + 入库
- **触发**:M5 通过。
- **规则**:财务出款打到客户账户,**必填银行流水号、扣减金额明细**(决策项 3,L-附);`paid_out=1`;车辆 `status='在库'`(若验车时为待维修则保持 `待维修`,修复后再回库)。
- **输出**:租赁流程结束,合同 `contract_status='已退车'`。

---

## 模块 N — 过户(以租代售结清 / 提前结清)

### N1 自然结清(决策项 18;v3 增补:contract_type 守卫)
- **触发(v3 修订)**:I 模块某一期核销后,系统**同时**满足以下三条:
  1. **`contracts.contract_type = '销售'`**(即以租代售;v3 新增,**租赁合同不进入 N1**)。
  2. `Σ paid_amount ≥ Σ amount`。
  3. `period == loan_periods`。
- **修订原因**:v2 触发条件不区分业务类型,租赁合同(车要还回来)末期还完款会被错误生成过户单,与 M 模块退车流程冲突。v3 加守卫后:
  - `contract_type='销售'` → 进入 N1,自动开过户单,推运营。
  - `contract_type='租赁'` → 末期核销完成后,合同保持 `执行中`,等销售在 M1 发起退车,**不进入 N1、不生成 ownership_transfers**。
- **幂等键**:`ownership_transfers` 表新增 `idempotency_key = sha256(contract_id + 'natural_settle')`,UNIQUE 约束;同一合同重复触发只生成一条过户单。
- **规则**:
  1. 财务复核结清,**前置**:必须先完成 J3(若有未减免滞纳金)。
  2. **⑧ 结清前关闭审核中的减免单**:创建过户单前,系统检查该合同是否存在 `waivers.status ∈ ('待审批','已通过')` 的未终结减免单——
     - **存在 → 阻断结清**,提示"该合同有 N 笔减免审批未终结,请先完成审批或撤销",**不创建过户单**;
     - 待所有减免单走到终态(`已生效`/`已驳回`/`已撤销`)后才允许结清。
     - **目的**:避免"已开过户单"后老板又批准/驳回减免,导致结清金额与过户状态错位、滞纳金重复冲销。
  3. 创建 `ownership_transfers(status='待过户', idempotency_key=...)`,推送运营。
- **代码改造提示**:I2 末期核销触发 N1 的位置需先读 `contracts.contract_type`,只有 `销售` 才创建过户单;创建前增查 in-review `waivers` 守卫(⑧)。

### N2 提前结清买断(决策项 19)
- **触发**:运营端"计算剩余应还本金"。
- **公式(展开版)**:
  ```
  remaining_principal = Σ_{未还期} amount             # 剩余应还本金
                     - 已审批减免金额                # 来自 K3/J3
                     + 未减免滞纳金累计              # 来自 J1 ledger
                     + 未付清应收(保险/维修/事故/罚金)# 来自 contract_fee_items 应收差
                     - 客户预收余额                  # 来自 customer_prepayments
  ```
  > **注(v3.1-v2)**:本公式仅 `客户预收余额`(客户真金白银多付、挂账的钱)参与冲减。**以租代售无押金项;首付款为购车本金,已在 F1 作为独立应还行计入,不在此重复扣减、也不可挪用顶欠。** 押金仅租赁退车(M 模块)结算时处理,不进入本公式。
- **流程**:
  1. 运营计算并出报价 → 与客户确认。
  2. 客户一次性支付 → 运营上传客户截图(必填) → 财务确认收款(必填银行流水号,选填公司截图) → 写 `contracts.early_settlement_amount/early_settled_at/early_settlement_serial`。
  3. 触发 N3 过户。
- **幂等**:与 N1 类似,`idempotency_key = sha256(contract_id + 'early_settle')`。

### N3 过户登记
- **触发**:N1 / N2 完成后。
- **输入**:过户日期、新车主名称、新车主身份证、过户材料附件(行驶证、登记证)。
- **规则**:
  - 写 `ownership_transfers(status='已过户', transfer_date)`。
  - 车辆 `status='已售/已过户'`,合同 `contract_status='已结清'` 或 `已提前结清`。
- **异常**:材料不全的过户单不能完结,可挂"待过户"长期跟踪。

---


## 模块 O — 车辆返利

### O1 财务录入
- **触发**:财务端"返利录入"。
- **输入**:车辆 VIN、关联合同(可选)、返利类型(厂家返利/区域返利/其它)、金额、到账日期、附件、备注。
- **规则**:写 `vehicle_rebates(created_by=...)`。
  > **⑨ 修订——去掉表内 `visibility` 冗余字段**:v2 在 `vehicle_rebates` 上设 `visibility='finance_boss'` 做行级可见控制,与模块 U 的字段级权限矩阵职责重叠、两处维护易不一致。v3 **删除该字段**,可见性**统一由 U2 矩阵**(`vehicle_rebates.*` 仅财务 R/W、老板 R,其余角色无权)+ O2 路由层校验承载,单一事实来源。

### O2 可见范围(决策项 32)
- **可见角色**:**仅财务、老板**。**销售/运营/车管完全不可见**(列表与详情都过滤,API 路由层校验角色)。
- **接口**:`GET /api/vehicle-rebates` 在路由层校验角色;返回 403 给非授权角色。

### O3 报表
- 老板穿透看板:按月/车型/车辆维度汇总;计入"公司报表",**不进入单车盈利展示给销售/运营**。

---

## 模块 P — 单车盈利统计(决策项 32)

### P1 基础数据生成(v3.1-v2:以"月度现金流表"为单车盈利核心算法)
- **触发**:E2 已激活后(F4 → E2 通过)自动触发。
- **数据来源**:车辆采购价、税率、保险、计划收入(客户分期表)、厂家计划支出(厂家分期表)、其它费用项。
- **核心算法——月度现金流对齐(与 F3 共用同一张表)**:把"厂家给公司的分期(流出,负)"与"公司给客户的分期(流入,正)"铺在同一条月份轴上,逐月求**每月净流水**与**累计净流水**,无需强制按期对账;**最终累计净流水 = 客户分期总额 − 厂家分期总额 = 该车单车毛利**。期数不同(如客户 6 期 / 厂家 12 期)由月份轴自然消化,不再要求逐期一一对应。
  > 示例见 F3:a 车厂家 12 期×-10000、客户 6 期×+25000,累计净流水收敛于 +30000 = 150000 − 120000,即单车毛利。
- **输出**:写盈利底表(可基于现 `/api/profit/by-vehicle` 视图扩展),并保留月度现金流明细供 P2/F3 穿透。

### P2 财务/老板统计
- **可见角色**:**仅财务、老板**。
- **维度**:单车、客户、车型、合同。
- **指标**:计划收入、已收、未收、滞纳金应收、滞纳金减免、返利、毛利、净利。

### P3 销售/运营视图
- **决策项 32**:销售、运营**完全不展示**采购价、税率、返利、毛利、净利。
- 销售/运营仅可看自己负责合同的"客户视角"数据:计划收入、已收、未收、滞纳金应收、减免明细。
- 模块 U 字段级权限矩阵兜底。

---

## 模块 Q — 老板穿透看板(决策项 34)

### Q1 总览卡(时间维度统一)
- 在租车辆数 / 在售(以租代售)车辆数 / 在库 / 待维修
- **当月**计划回款 / 实际到账 / 逾期金额 / 滞纳金累计
- **当月**返利 / **当月**毛利

> **决策项 34**:**所有"当月"维度统一为"自然月 + 应还日为锚"**——
> - "当月计划回款" = `repayments WHERE due_date in [本月 1 日, 本月末日]`。
> - "当月实际到账" = `bank_serial.verified_at in [本月 1 日, 本月末日]`(以财务核销时间)。
> - "当月逾期金额" = `repayments WHERE due_date < today AND status != '已还款'` 在本月末时点的快照。
> - "当月返利" / "当月毛利" 同样以自然月切分。

### Q2 待办
- 待价格特批的报单数(SLA 超时高亮)
- 待退车领导审批数
- 待租金/滞纳金减免审批数
- 指导价缺失车辆数
- 黑名单待审核数(模块 S)

### Q3 穿透
- 任一报单 → 报价单 → 合同 → 客户表/厂家表 → 付款 → 出库 → 还款 → 催款 → 扣款 → 退车 → 过户 → 盈利 → 返利
- 任意一行均可点击下钻;所有金额可导出 Excel。
- 时间筛选:自然月 / 自然季 / 自然年 / 自定义区间(应还日维度)。

---


## 模块 R — 收款公司主数据(新增,决策项 23)

### R1 维护
- **触发**:老板端 `/admin/payment-companies`。
- **输入**:公司全称、简称、社会统一信用代码、银行名称、银行账号、开户行、纳税人识别号、是否启用、备注。
- **规则**:
  - 同一全称 + 银行账号 不可重复(UNIQUE)。
  - 启用状态可切换;停用后销售下拉不再展示,但已关联合同保持原值。
- **数据表**:
  ```sql
  CREATE TABLE payment_companies (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    short_name TEXT,
    credit_code TEXT,
    bank_name TEXT NOT NULL,
    bank_account TEXT NOT NULL,
    bank_branch TEXT,
    tax_no TEXT,
    is_active INTEGER DEFAULT 1,
    remark TEXT,
    created_at TEXT,
    updated_at TEXT,
    UNIQUE(name, bank_account)
  );
  ```

### R2 联动
- 销售报单(D3)收款公司下拉读取 `payment_companies WHERE is_active=1`。
- T-3 提醒文案中的"收款公司"占位符自动从该表填充。
- 发票申请(L1)收款公司联动该表。
- 提前结清(N2)收款账户联动该表。

### R3 审计
- 任何字段变更写 `audit_logs`,操作人、字段差异落库。

---

## 模块 S — 客户黑名单(新增,决策项 27)

### S1 录入
- **触发**:老板/财务端"加入黑名单"。来源:历史合同欠款 / 失联 / 严重违约。
- **输入**:客户姓名、身份证号、电话、加入原因、附件证据(可选)、限制等级(警示/拒绝)。
- **规则**:
  - 身份证号 + 电话作为唯一索引。
  - **限制等级**:
    - `警示`:销售可报单,但提交时弹窗警告 + 老板审批节点。
    - `拒绝`:销售直接无法报单(D3 提交即拒)。
- **数据表**:
  ```sql
  CREATE TABLE customer_blacklist (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    customer_name TEXT NOT NULL,
    id_card TEXT NOT NULL,
    phone TEXT,
    reason TEXT NOT NULL,
    level TEXT NOT NULL CHECK(level IN ('警示','拒绝')),
    evidence_path TEXT,
    created_by TEXT NOT NULL,
    created_at TEXT NOT NULL,
    revoked_by TEXT,
    revoked_at TEXT,
    revoke_reason TEXT,
    UNIQUE(id_card, phone)
  );
  ```

### S2 审核与解禁
- **触发**:销售/客户申诉 → 老板端"黑名单审核"。
- **规则**:老板可解禁,需填解禁原因;解禁后 `revoked_at/revoked_by/revoke_reason` 落库,但记录保留(供历史追溯)。

### S3 联动
- D3 报单提交时校验:命中 `level='拒绝'` 直接报错;命中 `level='警示'` 弹窗"该客户在黑名单警示中,继续提交将进入老板审批",并把该报单优先级置高。
- 老板看板 Q2 显示"黑名单待审核数"。

---

## 模块 U — 字段级权限矩阵(新增,决策项 32、36)

### U1 设计原则
- 角色 → 字段 → 操作(读/写),三维矩阵。
- 默认拒绝,显式授权。
- 后端 API 在 DTO 序列化层做兜底过滤,前端按矩阵渲染表单/列表。

### U2 关键字段权限矩阵(节选)

| 实体.字段 | 销售 | 财务 | 运营 | 车管 | 老板 |
|---|---|---|---|---|---|
| `vehicles.purchase_price`(采购价) | — | R/W | — | — | R/W |
| `vehicles.tax_rate`(税率) | — | R/W | — | — | R/W |
| `vehicles.estimated_residual_value`(残值) | — | R | R | — | R |
| `vehicle_rebates.*`(返利) | — | R/W | — | — | R |
| `profit.gross_profit`(毛利) | — | R | — | — | R |
| `profit.net_profit`(净利) | — | R | — | — | R |
| `sales_orders.snapshot_*`(指导价快照) | R(自己) | R | R | — | R |
| `factory_repayments.amount`(厂家计划金额)〔注1〕 | — | R/W | — | — | R |
| `late_fee_ledger.*` | R(自己) | R/W | R(自己) | — | R |
| `waivers.*`(减免) | R/W(申请) | R/W(执行) | R | — | R/W(审批) |
| `invoice_requests.*` | R | R/W | R/W(申请) | — | R |
| `customer_blacklist.*` | R(命中) | R/W | — | — | R/W |
| `payment_companies.*` | R(下拉) | R | R | — | R/W |
| `contracts.early_settlement_amount` | R | R/W | R/W | — | R |
| `lock_requests.*` | R/W | R | R | — | R/W(审批) |

> 〔注1〕厂家计划金额取 `factory_repayments.amount`。v2 曾误写为 `repayments.factory_amount`,与"客户表=`repayments`、厂家表=`factory_repayments`"的双表分离设计冲突,v3 订正(⑫)。

### U3 实现
- 矩阵存于 `field_permissions` 表(可由老板维护界面调整,沿用 `role_actions` 模式)。
- 后端中间件:`@require_field_perm('vehicles.purchase_price', 'read')` 装饰器。
- 前端组件:`<FieldGuard entity="vehicles" field="purchase_price" op="read">` 包裹。

---

## 模块 V — 非功能要求(幂等 / SLA / 时间口径,决策项 18、20、33、34)

### V1 幂等性(决策项 18、33)
- **核心写动作必须幂等**:
  - 报单提交:`sales_orders.idempotency_key = sha256(销售id + VIN + 起租日 + 提交时间戳分钟级)`。
  - 还款核销:`reconciliation_allocations.idempotency_key = sha256(repayment_id + bank_serial)`。
  - 自然结清/提前结清:见 N1/N2。
  - 发票开具:`invoice_requests.idempotency_key = sha256(contract_id + period + invoice_no)`。
  - 滞纳金计提:`late_fee_ledger UNIQUE(repayment_id, accrued_date)`(已有)。
- **重复请求**:返回原结果(HTTP 200 + `idempotent_replay=true` 标识),不报错。
- **异常恢复**:cron 任务允许重跑,内部按幂等键去重。

### V2 SLA 超时提醒(决策项 20)

| 节点 | 超时阈值 | 处理 |
|---|---|---|
| 老板价格特批(E1) | 24h | 系统每日给老板推提醒 + 销售可催办 |
| 财务报单确认(E2) | 24h(F3 完成后) | 推送财务主管 |
| 老板减免审批(K2/J3) | 48h | 销售催办 + 看板红色 |
| 财务减免执行(K3/J3) | 24h | 推送财务主管 |
| 老板退车审批(M5) | 48h | 销售/财务催办 + 看板红色 |
| 财务出款(M6) | 财务审批后 3 工作日 | 看板黄色 → 红色升级 |
| T+3 运营催款(H2) | 当日内 | 次日提醒运营主管 |
| T+7 销售催款(H2) | 当日内 | 次日提醒销售主管 + 老板看板 |
| 财务开票(L2) | 申请后 3 工作日 | 推送财务主管 |
| 过户登记(N3) | 结清后 30 日 | 推送运营主管 |
| **退车场景 J3 减免审批(v3 新增)** | **24h** | **推送老板 + 销售主管;48h 仍未处理 → 默认按"不减免"放行 M3,事后允许补审批冲正(避免 M3 永久卡死)** |
| **H3 滞纳金抵冲减免守卫(v3 新增)** | **48h** | **守卫超时 → 解除,按未减免抵冲;事后批准走"反向减免登记"冲正** |

- **实现**:cron 每日扫描各业务表 `created_at / submitted_at`,落 `sla_alerts` 表 → 工作台徽标 + 站内信。

### V3 时间口径(决策项 34)
- **统一口径**:
  - "当月" = 自然月(本月 1 日 0 时 ~ 本月末日 23:59:59)。
  - "应还" = 以 `repayments.due_date` 为锚。
  - "实收" = 以财务核销时间 `verified_at` 为锚。
  - "逾期天数" = `today - due_date`(忽略时分,按整日)。
- **数据库时间字段**:统一存 ISO 8601 字符串(`YYYY-MM-DD HH:MM:SS`),入库前 trim,展示前按客户端时区(默认 +08:00)。
- **跨月切换**:每月 1 日 00:30 cron 重算"上月闭口数据"快照,写 `monthly_snapshots`,Q 看板的"上月对比"读快照。

### V4 缓存与性能
- 老板看板首屏 < 2s:聚合查询缓存 5 分钟,任何写入操作触发对应主键失效。
- 单车盈利计算异步化:E2 激活后入队,5 分钟内生成,失败重试 3 次后告警。

### V5 数据备份与审计
- 数据库每日 02:00 全量备份,保留 30 日。
- `audit_logs` 不可删除,仅可压缩归档(超过 1 年迁移至冷库)。

---


## 附录:状态字典(v2 修订,决策项 24)

### 销售报单 `sales_orders.order_status`(简化)

| 值 | 含义 | 下一可达状态 |
|---|---|---|
| `草稿` | 销售保存未提交,7 天有效 | `待价格特批` / `待财务确认` / `已作废`(过期) |
| `待价格特批` | 价格低于指导价,等待老板 | `待财务确认` / `已作废`(驳回) |
| `待财务确认` | 待 F1~F3 比对完成后财务激活 | `已激活` / `已作废`(驳回) |
| `已激活` | 合同/计划/盈利底表均已落库 | — |
| `已作废` | 终止态(驳回、撤回、过期统一收口,审计区分) | — |

> v2 删除了 `价格特批驳回`、`财务驳回`、单独的 `已作废`,合并为 `已作废` + 操作日志。

### 合同 `contracts`

| 字段 | 取值 |
|---|---|
| `contract_status` | `执行中` / `退车中` / `已退车` / `已结清` / `已提前结清` |
| `delivery_status` | `待审批` / `待首付款` / `首付已驳回` / `待出库` / `已出库` |
| `down_payment_status` / `deposit_status` | `待收` / `已收` / `免收` |

> **`delivery_status` 流转(本版补全)**:`待审批 →(审批通过)→ 待首付款 →(首付款审批驳回)→ 首付已驳回 →(重新提交)→ 待首付款 →(首付款到账确认)→ 待出库 →(车管出库)→ 已出库`。原字典遗漏 `待首付款`、`首付已驳回` 两态(代码 `app.py:2422/3256` 已在用),本版补入,与 G2/G3 出库门禁对齐。

### 车辆 `vehicles.status`(决策项 6、24)

`在库` / `报单锁定中` / `租赁中` / `以租代售` / `维修中`(合同存续期内事故修) / `退车中`(M1~M5) / `待维修`(M2 验车判定) / `二手车` / `已售/已过户`

### 车辆锁定 `vehicles.lock_status`(本版新增,见 H4)

`未锁` / `锁车流程中`(销售申请→运营→老板审批中,等销售线下操作) / `车辆已锁`(销售第三方系统锁车成功后点【锁车】确认) / `开锁流程中`(解锁审批中,等销售线下操作)

> **与 `vehicles.status` 解耦**:锁定状态是独立维度,不改变 `在库/租赁中/以租代售` 等主状态;一辆 `租赁中` 的车可同时为 `车辆已锁`。平台不远程控车,`车辆已锁`/`未锁` 终态由销售在第三方系统操作成功后点确认按钮落定(H4)。

### 还款 `repayments.status`(决策项 24)

| 值 | 含义 |
|---|---|
| `未激活` | F1 落库默认值;报单未经 E2 激活前的占位态,**不被 H1 扫描、不计滞纳金、不推催款**(②) |
| `待还款` | F4 经 E2 激活后由 `未激活` 整表转入,进入 H1 监控 |
| `临近还款` | T-3 / T-2 / T-1 系统标记(`-3 ≤ diff ≤ -1`),三天均预警 |
| `还款日` | T 日(`diff = 0`)当期到期 |
| `已还款` | 当期等额或多还核销完成 |
| `部分核销` | 客户已部分支付,但当期未结清 |
| `预抵` | 由前期客户真实多还的预收抵扣覆盖(非押金/首付款) |
| `逾期N日` | `diff ≥ 1` 且当期未结清,逐日命名(`逾期1日`、`逾期2日` …),欠款 + 万5/日滞纳金持续累计,直至客户真实还清。`diff=3` 时推【运营催款】、`diff=7` 时推【销售催款】,但状态命名始终是 `逾期N日`,不因催款节点改变 |

> **v3.1-v2 按 PDF 口径统一重写**:取消旧版按业务模式细分的 `资金池可冲/耗尽/无可抵冲` 与 `待催款`、`T+3运营已催`、`T+7销售已催` 等态。逾期一律走逐日 `逾期N日` 单一命名,**租赁与以租代售逾期处理完全一致**;首付款与押金任何节点都不参与抵冲。催款只是 `diff=3`(运营)/`diff=7`(销售)触发的任务推送,不再单独占用状态值。`未激活` 仍隔离 F1 落库到 F4 激活之间的窗口(②)。

### 退车 `return_inspections.status`

`待登记` / `待车管验车` / `待运营填写` / `待财务复核` / `待领导审批` / `待出款` / `已完成`

### 过户 `ownership_transfers.status`

`待过户` / `已过户`

### 减免 `waivers.status`(决策项 30,表名重命名)

`待审批` / `已通过` / `已生效` / `已驳回` / `已撤销`

### 发票 `invoice_requests.status`(决策项 28、29)

`待老板审批`(大额发票) / `待开票` / `已开票` / `已作废` / `已红冲` / `待重开`

### 黑名单 `customer_blacklist.level`

`警示` / `拒绝`

### 黑名单状态(由 `revoked_at` 推导)

`生效中`(`revoked_at IS NULL`) / `已解禁`(`revoked_at IS NOT NULL`)

---

## 附录 W:建表/迁移清单(v3 新增)

> **核验依据**:对照 `database.py` 当前 schema,v3 引用的下列表/列均尚未落地。本清单按"是否已存在 / 优先级 / 依赖关系"组织,供研发排期。

### W1 全新建表(14 张待建 + 1 张废弃,按 P0/P1/P2 优先级)

> **数量口径(本版统一)**:下表共列 15 行,其中 #1~#14 为**待建表(14 张,经 `database.py` 实测均缺失)**,#15 `rent_waivers` 为**废弃项(本就不存在,直接建 `waivers` 取代)**。原文档标题"15 张"、变更日志 ⑧"13 张"两处数字不一致,本版统一更正为"14 张待建 + 1 张废弃"。

| # | 表名 | 现状 | 优先级 | 用途 | 依赖 |
|---|---|---|---|---|---|
| 1 | `waivers` | 缺 | **P0** | J3/K 减免审批主表(原 v2 提到的 rent_waivers 也不存在,直接 CREATE) | contracts |
| 2 | `late_fee_ledger` | 缺 | **P0** | 滞纳金每日计提台账 | repayments |
| 3 | `customer_prepayments` | 缺 | **P0** | 客户预收余额(I2 多还抵不完的部分) | contracts |
| 4 | `ownership_transfers` | 缺 | **P0** | 过户单(N1/N2 写入) | contracts |
| 5 | `invoice_requests` | 缺 | **P0** | 发票申请/作废/红冲 | contracts |
| 6 | `notification_logs` | 缺 | P1 | T-3 提醒及催款通知留痕 | repayments |
| 7 | `vehicle_rebates` | 缺 | P1 | 车辆返利(仅财务/老板可见,⑨:不含 visibility 列) | vehicles |
| 8 | `payment_companies` | 缺 | P1 | 收款公司主数据(模块 R) | — |
| 9 | `customer_blacklist` | 缺 | P1 | 客户黑名单(模块 S) | customers |
| 10 | `field_permissions` | 缺 | P2 | 字段级权限矩阵落库(模块 U) | users/roles |
| 11 | `sla_alerts` | 缺 | P2 | SLA 超时提醒任务表(模块 V) | — |
| 12 | `monthly_snapshots` | 缺 | P2 | Q 看板月度快照(决策项 34) | — |
| 13 | `role_pages` | 缺 | P1 | 角色→可见页面映射(A1 登录返回 `role_pages`,⑩:database.py 仅有 users 表,该映射无处落库) | users/roles |
| 14 | `role_actions` | 缺 | P1 | 角色→可执行动作映射(A1 返回 `role_actions`、U3 引用"沿用 role_actions 模式",同样缺表,⑩) | users/roles |
| 15 | ~~`rent_waivers`~~ | 不存在 | **删除** | v2 的 RENAME 方案弃用,直接建 `waivers`(见 J3) | — |

> **⑩ 说明**:A1 登录接口(本文档第 48 行)返回 `role_pages`、`role_actions`,U3 又写"沿用 `role_actions` 模式",但 `database.py` 当前 `users` 表只有 `id/username/password/display_name/role/is_active`,**这两张映射表从未建过**。v3 在此显式补建,避免"接口返回不存在的字段"。若短期不落库,A1 应改为仅返回 `role`,前端按 `role` 静态映射页面/动作——二选一,本清单选择补表以支持老板可维护。

> **已存在表**:`lock_requests`(模块 H4)、`approval_flows`(模块 D/E/M)。继续沿用,不需要新建。

### W2 已有表追加列(`repayments`)

| 列名 | 类型 | 用途 | v3 备注 |
|---|---|---|---|
| `extra_alloc_confirmed_by` | TEXT | I1 多还抵扣月份确认人 | P0 |
| `extra_alloc_confirmed_at` | TEXT | I1 确认时间 | P0 |
| `extra_alloc_periods` | TEXT(JSON) | 抵扣覆盖的期数列表 | P0 |
| `idempotency_key` | TEXT UNIQUE | 跨重试幂等(决策项 33) | P1 |
| ~~`plan_kind`~~ | — | **删除** v2 提案 | v3 维持双表设计,客户表=repayments、厂家表=factory_repayments,plan_kind 字段冗余 |
| ~~`factory_amount`~~ | — | **删除** v2 提案 | 该字段属于 factory_repayments.amount,不应放在 repayments(U2 矩阵 v3 已修正) |

### W3 已有表追加列(`invoice_requests`,L3 红冲)

```sql
ALTER TABLE invoice_requests ADD COLUMN voided_by TEXT;
ALTER TABLE invoice_requests ADD COLUMN voided_at TEXT;
ALTER TABLE invoice_requests ADD COLUMN void_reason TEXT;
ALTER TABLE invoice_requests ADD COLUMN previous_invoice_id INTEGER;
ALTER TABLE invoice_requests ADD COLUMN red_invoice_no TEXT;
ALTER TABLE invoice_requests ADD COLUMN red_invoiced_at TEXT;
ALTER TABLE invoice_requests ADD COLUMN red_reason TEXT;
ALTER TABLE invoice_requests ADD COLUMN red_certificate_path TEXT;
-- 注:invoice_requests 整张表本身需先 CREATE(见 W1 #5)
-- v3 修订:invoice_no 字段允许 NULL(L1 申请时尚无),仅在 L2 开票后强制非空
--         CREATE 时:invoice_no TEXT(允许 NULL,UNIQUE 约束在 SQLite 下允许多个 NULL)
```

### W4 数据迁移注意事项

- **存量"逾期"状态值清理(v3.1-v2 口径)**:`check_overdue` 旧逻辑生成的 `repayments.status='逾期'`,迁移时按当日 `diff = today − due_date` 一次性回填为逐日 `逾期N日`(`diff` 多少即 `逾期{diff}日`),**不区分业务模式、不再生成 `逾期-资金池可冲/耗尽/无可抵冲` 等已废弃值**。已落库的旧资金池状态一并按 `diff` 重算覆盖。
- **bank_serial 唯一性变更(决策项 31 + v3 修订)**:由"维度内唯一"改为"(bank_serial, contract_id, period) 复合幂等键",允许同一笔流水分摊到多期登记。需检查并清理违反新约束的存量数据。
- **contracts 写回回填**:对历史已审批 `contract_initial_payments` 而 contracts 字段未同步的记录,执行一次性补写脚本。

---

## 变更日志

### v2.0(2026-05-28)
基于 v1 Review 报告 36 条决策的全量修订,关键变化:

| # | 变更 | 影响模块 |
|---|---|---|
| 1 | F/E 时序解耦:F1~F3 前置于 E2 | E、F |
| 2 | 滞纳金减免归一为 J3,M1 不再单点决策 | J、M |
| 3 | 截图规则修订:运营/销售必填,财务侧选填 | G、H、I、L、L-附 |
| 4 | 合同表删除双指导价快照,只在报单层留存 | G |
| 5 | T+7 押金/首付款抵冲 + 滞纳金按原始未还本金计提 ~~押金/首付款抵冲~~ **【v3.1-v2 废止:首付款/押金一律不可抵扣,仅滞纳金按原始未还本金计提部分保留】** | H、J |
| 6 | 车辆状态机修订:`待维修` 仅由 M2 进入 | B |
| 7 | 多还抵扣月份在 I1 由运营+客户确认 | I |
| 8 | 报单状态简化:驳回统一为 `已作废` | D、E、附录 |
| 9 | 指导价缺失硬性阻塞报单提交 | C、D |
| 10 | 草稿保存路径明确,7 天过期 | D |
| 11 | F3 跨期归一化比对公式 | F |
| 12 | J3 减免与 H3 抵冲互斥 | H、J |
| 13 | ~~抵冲累计上限 = 资金池余额,清零即停~~ **【v3.1-v2 废止:取消资金池抵冲模型,首付款/押金不可抵扣,欠款仅由客户真实还款递减】** | H、I |
| 14 | 还款日规则租赁/以租代售明示 | F |
| 15 | 财务责任口径限定到流水/金额,不决策抵扣月 | I |
| 16 | M1 提前退车违约金输入字段 | M |
| 17 | 老板特批驳回 = 直接作废,不可重提 | D、E |
| 18 | N1/N2 幂等键防重 | N、V |
| 19 | N2 提前结清公式展开 | N |
| 20 | SLA 超时提醒矩阵 | V |
| 21 | T+7 后只提示锁车,不自动锁 | H |
| 23 | 新增模块 R 收款公司主数据 | R |
| 24 | 状态字典清理(`退车中`、`维修中`、逾期细化) | 附录 |
| 25 | 流水号/发票号长度规则 | L-附 |
| 26 | 草稿快照仅在提交时落库 | D |
| 27 | 新增模块 S 客户黑名单 | S |
| 28 | L1 发票申请前置审批(已收款 + 大额) | L |
| 29 | 区分作废 vs 红冲 | L、附录 |
| 30 | `rent_waivers` 重命名为 `waivers`,统一租金/滞纳金 | J、K |
| 31 | 流水号按维度去重 | L-附 |
| 32 | 销售/运营完全不见盈利/采购价/返利 | A、O、P、U |
| 33 | 新增模块 V 非功能要求(幂等 NFR) | V |
| 34 | Q 看板时间口径统一为自然月 + 应还日锚 | Q、V |
| 35 | 出库照片至少 3 张 + 拍摄部位提示 | G、M |
| 36 | 新增模块 U 字段级权限矩阵 | U |

> 决策项 22 暂不纳入(客户管理独立模块延后规划)。

---

### v3.0(2026-05-29)

基于 [v2 Review 报告](功能描述文档-5.27-v2-Review报告.md) 14 条真问题 + 1 条文档补充的全量修订。**不引入新决策项,仅修复 v2 落地缺陷与内部矛盾**。

| # | v2 问题分类 | v3 修订 | 影响模块 |
|---|---|---|---|
| ① | 严重/自相矛盾 | F3 维度 2(归一化公式)改为展示项不参与判定,避免与"允许利差"前提打架 | F |
| ② | 严重/自相矛盾 | ~~H1 扫描清单更新到 `逾期-资金池可冲/耗尽`、`逾期-无可抵冲`,`check_overdue` 重定义为 diff 区间式状态机(临近还款/待催款/T+3运营已催/T+7销售已催/终态)~~ **【已被 v3.1-v2 取代:改为逐日 `逾期N日` 状态机,取消资金池三态与待催款,详见本表 v3.1-v2 第 4 条】** | H |
| ③ | 严重/自相矛盾 | ~~H3 抵冲前增加"减免审批中"守卫,避免 T+7 抵冲与事后减免重复扣资金池~~ **【已被 v3.1-v2 取代:取消资金池自动抵冲,系统任何节点不动押金/首付款】** | H、J |
| ④ | 规则缺口 | ~~H3 抵冲池按 `contract_type` 区分:**租赁**=押金+客户预收可抵冲;**以租代售**=首付款无抵冲池~~ **【已被 v3.1-v2 取代:首付款与押金一律不可抵扣,租赁与以租代售逾期处理完全一致,欠款仅由客户真实还款递减】** | H |
| ⑤ | 文档补充 | G2 财务侧补 contracts 三字段同步写回说明(代码已实现);状态值统一为"已通过" | G |
| ⑥ | 规则缺口 | N1 触发条件加 `contract_type='销售'` 守卫;租赁合同末期走 M 退车,不进入 N1 | N |
| ⑦ | 规则缺口 | I2 多还分支前置"先冲应收(保险/维修/滞纳金)再预抵未来期"铁规 | I |
| ⑧ | 实现偏差 | 文档末尾追加附录 W "建表/迁移清单",列出 14 张待建表(另 1 张 `rent_waivers` 废弃)的优先级与依赖,标注存量数据迁移注意事项 | 全局 |
| ⑨ | 实现偏差 | J3 减免表的 v2 `RENAME rent_waivers TO waivers` 改为直接 `CREATE TABLE waivers`(源表本不存在) | J |
| ⑩⑪⑫ | 实现偏差 | 双表设计统一:U2 矩阵 `repayments.factory_amount` 改为 `factory_repayments.amount`;F1 删除 `plan_kind` 字段;repayments 仅追加 4 个真实需要的列 | F、I、U |
| ⑬ | 次要 | 发票号 UNIQUE 改为允许 NULL(L1 申请阶段无号),L2 开票时强制非空 + 全局唯一 | L、L-附 |
| ⑭ | 次要 | `repayments.bank_serial` 单列 UNIQUE 改为 `(bank_serial, contract_id, period)` 复合幂等键,支持"一笔款分摊多笔登记" | L-附 |
| ⑮ | 次要 | SLA 矩阵追加两行:退车场景 J3 减免审批 24h/48h 兜底、H3 抵冲守卫 48h 解除 | V |

**与 v2 的兼容性**:
- v3 不修改任何 v2 的决策项编号;v2 决策项实际为 **1~21、23~36 共 35 项**(决策项 22"客户管理独立模块"被搁置、未启用,编号空缺),v3 仅在此基础上修补条款。**注:决策项 5、13 在 v3.1-v2 已被废止(以删除线标注作废),但保留原编号占位、仍计入 35 项总数,不重新编号。**
- v2 中已正确描述的章节(模块 R/S/T/U/V 主体、L3 红冲流程、E2 财务确认、O 返利可见范围等)在 v3 中保持不变。
- 数据库迁移路径(v2 → v3,迁移口径已按 v3.1-v2 更新):按附录 W 顺序建表,把存量 `repayments.status='逾期'` 按当日 `diff = today − due_date` 一次性回填为逐日 `逾期N日`(不再按资金池余额改写、不再生成资金池相关状态值,详见附录"存量逾期状态值清理");清理违反 bank_serial 新约束的重复登记数据。

**未在 v3 修复的 v2 遗留项**:
- 决策项 22(客户管理独立模块)沿用 v2 暂不纳入的决定。
- 模块 P 单车盈利的厂家计划支出口径细化(v2 已描述但未给出公式),建议下版与财务对齐后落入 v4。

---

### v3.1(2026-05-30) — 业务方确认意见落定 ①~⑫

在 v3 基础上,按业务方逐条确认意见进一步落定 **12 项逻辑修订**。**仍不引入新决策项,仅锁定业务口径与守卫逻辑**。

| # | 修订内容 | 影响模块 |
|---|---|---|
| ① | F3 比对维度 2(归一化公式)改为**展示项不参与判定**,避免与"允许利差"前提打架 | F |
| ② | ~~H1 扫描清单更新到新状态;`check_overdue` 重定义为 **diff 区间式状态机**(临近还款/待催款/T+3运营已催/T+7销售已催/终态)~~ **【已被 v3.1-v2 取代:改为逐日 `逾期N日` 状态机,取消待催款与已催态】**;新增 `未激活` 态隔离 F1 载入但未经 F4/E2 激活的窗口,H1 不监控(此项保留有效) | H |
| ③ | ~~H3 抵冲前增加**"减免审批中"守卫**,避免 T+7 抵冲与事后减免重复扣资金池~~ **【已被 v3.1-v2 取代:取消资金池自动抵冲,系统任何节点不动押金/首付款】** | H、J |
| ④ | ~~H3 抵冲池按 `contract_type` 区分:**租赁**=押金+客户预收可抵冲;**以租代售**=首付款无抵冲池~~ **【已被 v3.1-v2 取代:首付款与押金一律不可抵扣,租赁与以租代售逾期处理完全一致】** | H |
| ⑤ | G2 财务侧补 contracts 三字段同步写回说明(代码已实现);合同状态值统一为**"已通过"** | G |
| ⑥ | N1 触发条件加 `contract_type='销售'` 守卫,租赁合同末期走 M 退车不进 N1;G3 出库门禁按业务模式区分(租赁=押金收齐+首期核销;以租代售=`down_payment_status='已收'`) | N、G |
| ⑦ | 发票**开票主体与合同主体解耦**,按申请填写开票,不强制一致 | L |
| ⑧ | N1 结清前**关闭审核中减免**,避免结清后减免悬挂 | N、J |
| ⑨ | `vehicle_rebates` 去掉 `visibility` 冗余字段,可见范围统一走 **U2 矩阵** | O、U |
| ⑩ | 附录 W 补 `role_pages`/`role_actions` 建表清单 | 全局、U |
| ⑪ | `waivers` 增 `(contract_id, waiver_kind, status)` 复合索引,支撑守卫查询 | J |
| ⑫ | 决策项计数订正为 **1~21、23~36 共 35 项**(22 空缺),清理变更日志陈旧措辞 | 全局 |

---

### v3.1-v2(2026-05-30) — 基于 5.30 Review 报告 + 业务方逐条确认

在 v3.1 基础上,按 [5.30 Review 报告](功能描述文档-5.30-Review报告.md) 与业务方逐条意见落定。**不引入新决策项,仅修复矛盾、补缺口、对齐代码与口径。**

| # | 修订内容 | 影响模块 |
|---|---|---|
| 1 | **H4 锁车/解锁重写为标记式审批流**:平台不远程控车;锁车=销售申请→运营通过→老板审批,过程态 `锁车流程中`,销售在第三方系统锁车成功后点【锁车】确认→ `车辆已锁`;解锁对称(`开锁流程中`→点【解锁】→`未锁`)。新增独立字段 `vehicles.lock_status`,与 `vehicles.status` 解耦 | H、B、附录 |
| 2 | **H1 触发方式落地**:统一入口 `POST /api/jobs/daily-collect`,APScheduler 每日 00:30 自动调 + 关键查询惰性兜底 + `(job,run_date)` 当天幂等/支持补跑;修正"端点不存在、仅看板被动触发"的实现偏差 | H |
| 3 | **H1 临近还款判定改对**:由 `diff ≤ -3`(误把远期单子全标临近)收窄为临近还款窗口;**最终口径以本表第 4 条为准(`-3 ≤ diff ≤ -1`,三天均预警),`diff < -3` 保持 `待还款`**。(本条原写 `-7 ≤ diff ≤ -3`,已被第 4 条 PDF 口径取代,此处订正避免冲突。) | H、附录 |
| 4 | **逾期模型重写为逐日 `逾期N日`(PDF 口径)**:取消 `待催款` 区间与按业务模式细分的 `资金池可冲/耗尽/无可抵冲` 三态。`-3≤diff≤-1`=临近还款(三天均预警)、`diff=0`=还款日、`diff≥1`=`逾期N日` 逐日命名;`diff=3` 推运营催款、`diff=7` 推销售催款,**租赁与以租代售逾期处理完全一致** | H、J、附录 |
| 5 | **F3 客户 24 期/厂家 36 期——以"月度现金流对齐"落定(PDF 口径)**:厂家分期(流出/负)与客户分期(流入/正)铺在同一月份轴,逐月求每月净流水 + 累计净流水,**最终累计 = 客户分期总额 − 厂家分期总额 = 单车毛利**,不再强制逐期勾稽;F3 与模块 P 共用同一张月度现金流表 | F、P |
| 6 | **退车流程确认**:销售发起退车→车管验车填单(M2)→运营查数据填单(M3)→财务复核(M4)→领导审批(M5)→出款入库(M6),与现有 M 模块一致;新增"租赁末期核销完成→系统在销售工作台生成待退车提示"作为 M1 触发锚点,避免合同/车辆停在末期不收口 | M、I |
| 7 | **状态字典补全**:`delivery_status` 补 `待首付款`/`首付已驳回`(代码 `app.py:2422/3256` 已在用);新增 `vehicles.lock_status` 字典 | 附录 |
| 8 | **附录 W 建表数统一**:订正为"14 张待建 + 1 张废弃(`rent_waivers`)",修正 W1 标题"15 张"与变更日志 ⑧"13 张"的口径不一致 | 全局 |
| 9 | **H1 代码改造提示补充**:`check_overdue` 实测有 3 个调用点(`app.py:1046/2085/2158`),重写后需逐一核对并收口到 `daily-collect` 单一入口 | H |
| 10 | **滞纳金起算明确**:逾期第 1 天(`diff = 1`,应还日次日)起按万5/日计提,**无宽限期**,与 H1 `diff ≥ 1` 分支对齐 | J、H |
| 11 | **T-3 提醒名单数据源统一**:工作台预警列表以 `status='临近还款'` 为唯一来源,前端不再各自用 `diff` 现算,杜绝"状态已临近但列表漏列" | H |
| 12 | **首付款/押金一律不可抵扣(PDF 口径)**:删除旧版 H3"押金 + 客户预收"组成的抵冲池及"T+7 顺序扣款顶欠款"逻辑。客户自 `diff=1`(T+1)起即按万5/日计提违约金,**唯一能减少欠款/计提基数的是客户真实到账还款**;押金仅在退车(M 模块)结算时扣除,首付款为购车本金、不可转移顶欠 | H、I、J、附录 |

> **本版已不再有"保留未决"项**:第 5 条(期数折算)已由月度现金流对齐落定;原"待催款"模型已被逐日 `逾期N日` 取代。三项 PDF 口径(逐日逾期、月度现金流、首付/押金不可抵扣)均已落入正文。

---

**文档结束**。后续若新增/修改流程,以新流程图为准并在本文件追加变更日志。
